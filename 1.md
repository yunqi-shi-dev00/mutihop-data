# 修复方案 🔧

## 目录
- [修复1：多跳JSON解析失败](#修复1多跳json解析失败)
- [修复2：测试0/4通过（问题表述改进）](#修复2测试04通过问题表述改进)
- [修复3：相关QA选择不合理](#修复3相关qa选择不合理)
- [修复4：全局JSON解析容错](#修复4全局json解析容错)
- [可选优化：语义embedding](#可选优化语义embedding)
- [修复优先级](#修复优先级建议)
- [测试验证](#测试验证)

---

## 修复1：多跳JSON解析失败

### 🔴 问题描述（最严重）

```
[多跳组合] 失败: Expecting value: line 7 column 3 (char 338)
[WARNING] 多跳生成失败
```

**影响**：SELECT操作完全失败，无法生成多跳问题

### 📝 修复方案

**文件**：`agent_final.py`

**位置**：`generate_multihop_question` 方法（约第265-295行）

#### 修改前

```python
async def generate_multihop_question(self, memory):
    """生成多跳问题"""
    num_hops = len(memory.relevant)
    if num_hops < 2:
        return None
    
    # ... 构建prompt ...
    
    text = await self.call_llm(prompt, temperature=0.7)
    
    # 直接解析JSON - 容易失败
    result = json.loads(text)
    
    return {
        'question': result['question'],
        'answer': result['answer'],
        'reasoning_steps': result.get('reasoning_steps', []),
        # ...
    }
```

#### 修改后（增强版）

```python
async def generate_multihop_question(self, memory):
    """生成多跳问题"""
    num_hops = len(memory.relevant)
    if num_hops < 2:
        return None
    
    # ... 构建prompt ...
    
    text = await self.call_llm(prompt, temperature=0.7)
    
    # ========== 增强JSON解析（3层容错） ==========
    result = None
    
    # 方法1: 直接解析
    try:
        result = json.loads(text)
        if self.debug_mode:
            print(f"    [解析] 方法1成功（直接解析）")
    except json.JSONDecodeError as e:
        if self.debug_mode:
            print(f"    [解析] 方法1失败: {e}")
        
        # 方法2: 提取```json```代码块
        try:
            if '```json' in text:
                json_text = text.split('```json')[1].split('```')[0].strip()
                result = json.loads(json_text)
                if self.debug_mode:
                    print(f"    [解析] 方法2成功（提取代码块）")
            else:
                raise ValueError("没有找到```json```标记")
        except Exception as e2:
            if self.debug_mode:
                print(f"    [解析] 方法2失败: {e2}")
            
            # 方法3: 正则提取第一个完整JSON对象
            try:
                import re
                # 匹配最外层的{}，支持嵌套
                json_match = re.search(
                    r'\{(?:[^{}]|\{(?:[^{}]|\{[^{}]*\})*\})*\}', 
                    text, 
                    re.DOTALL
                )
                if json_match:
                    result = json.loads(json_match.group())
                    if self.debug_mode:
                        print(f"    [解析] 方法3成功（正则提取）")
                else:
                    raise ValueError("正则未匹配到JSON对象")
            except Exception as e3:
                if self.debug_mode:
                    print(f"    [解析] 方法3失败: {e3}")
                    print(f"    [原始输出前500字符] {text[:500]}...")
                return None
    
    # 检查解析结果
    if result is None:
        if self.debug_mode:
            print(f"    [解析] 所有方法都失败")
        return None
    
    # 验证必需字段
    required_fields = ['question', 'answer', 'reasoning_steps']
    missing_fields = [f for f in required_fields if f not in result]
    
    if missing_fields:
        if self.debug_mode:
            print(f"    [解析] 缺少必需字段: {missing_fields}")
            print(f"    [解析] 当前字段: {list(result.keys())}")
        return None
    
    if self.debug_mode:
        print(f"    [解析] ✓ 成功，包含所有必需字段")
    
    return {
        'question': result['question'],
        'answer': result['answer'],
        'reasoning_steps': result.get('reasoning_steps', []),
        'quality_indicators': result.get('quality_indicators', {}),
        'metadata': result.get('metadata', {})
    }
```

### ✅ 修改步骤

1. 打开 `agent_final.py`
2. 找到 `generate_multihop_question` 方法
3. 定位到 `text = await self.call_llm(...)` 之后的部分
4. 完整替换为上述代码
5. 保存文件

### 📊 预期效果

- JSON解析成功率提升 **80%+**
- SELECT操作失败率大幅降低
- 调试模式下可以看到详细的解析过程

---

## 修复2：测试0/4通过（问题表述改进）

### 🔴 问题描述

```
[TEST] 正确率: 0/4
LLM的4次答案: "氧化铟的最大含量应该不超过80%"
标准答案: "氧化铟含量的上限阈值为88%"
```

**根本原因**：问题表述有歧义

```
问题: "如果氧化铟的含量达到80%，为了维持非晶结构特性，
      氧化铟的最大含量应该不超过多少？"
      ↑                                    ↑
   已经达到80%                         问最大含量
   
LLM错误理解: "已经到80%了，那最大就是80%"
正确理解: "上限阈值是88%"
```

### 📝 修复方案

**文件**：`prompts_final.py`

**位置**：`base_qa` 模板（约第14-38行）

#### 修改前（简单版）

```python
base_qa = '''你是一个半导体领域的多跳QA构建专家。基于给定的半导体知识材料，对问答对语法优化。确保问题清晰、可解、答案唯一。

# 半导体知识材料
{qa_entity}

输出JSON格式：
```json
{{
    "question": "针对原问题语法改善后的问题",
    "answer": "针对原始答案语法改善后的问题答案",
    "statement": "该问答对涉及的核心事实陈述",
    "key_concepts": ["关键概念1", "关键概念2"]
}}
```
'''
```

#### 修改后（增强版，强调清晰性）

```python
base_qa = '''你是一个半导体领域的多跳QA构建专家。基于给定的半导体知识材料，对问答对语法优化。确保问题清晰、可解、答案唯一。

# 半导体知识材料
{qa_entity}

## 【核心要求】

### 1. 问题改写原则（严格执行）

**(1) 清晰性 - 避免歧义表述**
- 问题表述必须明确、无歧义
- **禁止使用**："如果...达到X%，最大是多少"这类表述
- **推荐使用**：直接询问"阈值"、"上限"、"范围"

**(2) 可解性**
- 问题必须基于给定答案可直接回答
- 不引入答案中没有的信息
- 确保问答逻辑一致

**(3) 答案唯一性**
- 问题应有唯一正确答案
- 避免模糊的多解释空间

### 2. 常见错误示例与正确改写

#### ❌ 错误示例1：涉及阈值/上限的歧义表述

```
原问题: "当氧化铟含量达到80%时，为维持非晶结构，氧化铟含量上限应控制在多少？"

❌ 错误改写:
"如果氧化铟的含量达到80%，氧化铟的最大含量应该不超过多少？"
                ↑
问题：容易理解成"已经到80%了，那最大就是80%"
（但正确答案是88%）

✅ 正确改写:
"在IGZO薄膜晶体管中，为维持非晶结构特性，氧化铟含量的上限阈值是多少？"
                ↑
优点：直接询问上限，清晰无歧义
```

#### ❌ 错误示例2：温度范围的歧义表述

```
原问题: "退火温度从200°C开始，最高可加热到多少？"

❌ 错误改写:
"如果温度达到200°C，最高温度应该不超过多少？"
问题：容易理解成"最高就是200°C"

✅ 正确改写:
"在退火工艺中，温度的上限阈值是多少？"
或
"退火工艺的最高温度是多少？"
```

#### ✅ 正确示例：保持原意，语法优化

```
原问题: "IGZO薄膜中ZnO含量对透过率的影响？"

✅ 改写:
"在IGZO薄膜材料中，ZnO含量如何影响可见光透过率？"
优点：保持原意，语法更清晰，无歧义
```

### 3. 改写检查清单（自检）

在输出前，必须自我检查：

- [ ] 问题是否直接、明确？
- [ ] 是否避免了"如果...达到X，最大/最小是多少"这类表述？
- [ ] 如果涉及阈值/上限/范围/极值，是否直接询问数值？
- [ ] LLM看到这个问题，能否直接给出正确答案（无歧义）？
- [ ] 问题和答案是否逻辑一致？
- [ ] 问题是否独立完整（不依赖上下文）？

### 4. 特殊情况处理

**(1) 如果原问题确实是"从X到Y的范围"**

```
原答案: "氧化铟含量范围为50%-88%"

✅ 改写:
"在IGZO薄膜晶体管中，氧化铟含量的有效范围是多少？"
不要拆分成"下限是多少"或"上限是多少"
```

**(2) 如果原问题涉及"临界值"**

```
原答案: "当ZnO含量超过71.4%时，透过率显著下降"

✅ 改写:
"在IGZO薄膜中，ZnO含量的临界阈值是多少？超过该值会导致什么问题？"
明确询问临界值，并说明后果
```

## 【输出格式】

```json
{{
    "question": "针对原问题语法改善后的问题（清晰、无歧义、可直接回答）",
    "answer": "针对原始答案语法改善后的问题答案",
    "statement": "该问答对涉及的核心事实陈述",
    "key_concepts": ["关键概念1", "关键概念2"]
}}
```

注意：
- question必须清晰无歧义
- question和answer必须逻辑一致
- 输出有效的JSON格式
'''
```

### ✅ 修改步骤

1. 打开 `prompts_final.py`
2. 找到 `base_qa` 变量定义
3. 完整替换为上述模板
4. 保存文件

### 📊 预期效果

- 基础QA问题表述更清晰
- 测试通过率从 **0/4** 提升至 **≥2/4** （50%+）
- 减少LLM对问题的误解

---

## 修复3：相关QA选择不合理

### ⚠️ 问题描述

```
QA-6: IGZO沟道材料的氧化铟含量控制
QA-15: 铝栅极材料的小丘抑制
桥联statement: "都涉及材料和掺杂策略"

问题：沟道和栅极是不同的器件组件，强行桥联不合理
```

### 📝 修复方案A：添加桥联合理性检查（推荐）

#### 步骤1：添加新的prompt模板

**文件**：`prompts_final.py`

**位置**：在类的末尾添加（约第520行之后）

```python
    # ========== 桥联合理性检查模板 ==========
    check_bridge_validity = '''你是一个半导体领域的技术专家。判断两个QA之间的桥联是否合理。

# QA-1 信息
问题: {qa1_question}
答案: {qa1_answer}
关键概念: {qa1_concepts}

# QA-2 信息
问题: {qa2_question}
答案: {qa2_answer}
关键概念: {qa2_concepts}

# 桥联声明
{statement}

## 【判断标准】

### 合理的桥联应满足：

1. **技术相关性**
   - 两个QA涉及相同或相关的技术领域
   - 例如：都涉及TFT沟道、都涉及材料制备、都涉及同一工艺参数

2. **逻辑连贯性**
   - 从QA-1推理到QA-2有明确的逻辑路径
   - 不是简单的表面概念重叠

3. **知识深度**
   - 桥联能够揭示更深层的技术关联
   - 不是泛泛而谈的"都涉及半导体"或"都涉及材料"

### ❌ 不合理的桥联示例

**示例1（器件组件不同）**：
```
QA-1: IGZO沟道材料的氧化铟含量控制
QA-2: 铝栅极材料的小丘抑制
桥联: "都涉及材料和掺杂策略"
判断: ❌ 不合理
原因: 沟道和栅极是不同的器件组件，技术关联太弱
```

**示例2（领域跨度太大）**：
```
QA-1: TFT的迁移率优化
QA-2: 显示器的色域标准
桥联: "都与显示技术相关"
判断: ❌ 不合理
原因: 器件性能和显示效果关联太远
```

**示例3（表面概念重叠）**：
```
QA-1: IGZO的氧分压控制
QA-2: 钝化层的厚度设计
桥联: "都是薄膜制备工艺参数"
判断: ❌ 不合理
原因: 虽然都是工艺，但作用对象和机制完全不同
```

### ✅ 合理的桥联示例

**示例1（同一材料的不同工艺）**：
```
QA-1: IGZO薄膜的氧分压控制
QA-2: IGZO薄膜的退火温度优化
桥联: "氧分压和退火温度共同影响IGZO薄膜的氧空位浓度"
判断: ✅ 合理
原因: 同一材料（IGZO）的相关工艺参数，有明确的技术关联
```

**示例2（同一器件的相关性能）**：
```
QA-1: TFT的阈值电压控制
QA-2: TFT的迁移率优化
桥联: "阈值电压和迁移率共同决定TFT的开关特性和驱动能力"
判断: ✅ 合理
原因: 同一器件（TFT）的相关性能参数，有明确的物理关联
```

**示例3（因果链关系）**：
```
QA-1: IGZO薄膜的氧空位浓度
QA-2: IGZO TFT的载流子迁移率
桥联: "氧空位浓度直接决定载流子浓度，进而影响迁移率"
判断: ✅ 合理
原因: 有明确的因果链：氧空位 → 载流子 → 迁移率
```

## 【评分标准】

**相关性分数**（0-10分）：
- **9-10分**：强相关
  - 同一器件/材料/工艺的不同方面
  - 有明确的因果关系或依赖关系
  
- **6-8分**：中等相关
  - 相关技术领域
  - 有一定的技术关联
  
- **3-5分**：弱相关
  - 表面概念重叠
  - 只是都属于"半导体"或"材料"
  
- **0-2分**：不相关
  - 不同技术领域
  - 不同器件组件
  - 没有实质关联

**合格线**：相关性分数 ≥ 6分

## 【输出格式】（严格遵循JSON）

```json
{{
    "judgement": "yes 或 no",
    "analysis": "分析说明（50字内，说明为何合理或不合理）",
    "relevance_score": 0-10
}}
```

**示例输出1（不合理）**：
```json
{{
    "judgement": "no",
    "analysis": "沟道材料和栅极材料属于不同器件组件，技术关联太弱",
    "relevance_score": 3
}}
```

**示例输出2（合理）**：
```json
{{
    "judgement": "yes",
    "analysis": "两者都涉及IGZO薄膜的工艺参数优化，有明确的技术关联",
    "relevance_score": 8
}}
```
'''
```

#### 步骤2：添加检查方法

**文件**：`agent_final.py`

**位置**：在类的方法区添加（约第700行左右）

```python
    async def check_bridge_validity(self, qa1: SemiconductorQAEntity, qa2: SemiconductorQAEntity, statement: str) -> dict:
        """检查两个QA之间的桥联是否合理"""
        prompt = SemiconductorQAPrompts.check_bridge_validity.format(
            qa1_question=qa1.qa_data.get('question', ''),
            qa1_answer=qa1.qa_data.get('answer', ''),
            qa1_concepts=', '.join(qa1.key_concepts) if qa1.key_concepts else '无',
            qa2_question=qa2.qa_data.get('question', ''),
            qa2_answer=qa2.qa_data.get('answer', ''),
            qa2_concepts=', '.join(qa2.key_concepts) if qa2.key_concepts else '无',
            statement=statement
        )
        
        text = await self.call_llm(prompt, temperature=0.3)
        
        # 解析结果
        try:
            # 尝试JSON解析
            result = json.loads(text)
            return {
                'is_valid': result.get('judgement', 'no').lower() in ['yes', 'yes'],
                'reason': result.get('analysis', ''),
                'relevance_score': result.get('relevance_score', 0)
            }
        except:
            # JSON解析失败，尝试简单文本判断
            if '【是】' in text or '"judgement": "yes"' in text.lower():
                return {
                    'is_valid': True,
                    'reason': '格式异常但判断为合理',
                    'relevance_score': 6
                }
            else:
                return {
                    'is_valid': False,
                    'reason': '格式异常或判断为不合理',
                    'relevance_score': 0
                }
```

#### 步骤3：在SELECT操作中使用

**文件**：`agent_final.py`

**位置**：`generate` 方法中的SELECT操作（约第600行）

找到这段代码：

```python
# (3) 构建link_qa
try:
    link_qa = await self.construct_link_qa(target, neighbor_entity)
except Exception as e:
    print(f"[WARNING] 构建link_qa失败: {e}")
    continue
```

在其后添加：

```python
# (3) 构建link_qa
try:
    link_qa = await self.construct_link_qa(target, neighbor_entity)
except Exception as e:
    print(f"[WARNING] 构建link_qa失败: {e}")
    continue

# ========== 新增：桥联合理性检查 ==========
try:
    bridge_validity = await self.check_bridge_validity(
        target, 
        neighbor_entity, 
        link_qa.get('statement', '')
    )
    
    if not bridge_validity['is_valid']:
        if self.debug_mode:
            print(f"    [桥联检查] ✗ 不合理 (分数: {bridge_validity['relevance_score']})")
            print(f"    [原因] {bridge_validity['reason']}")
        continue  # 跳过不合理的桥联
    
    if self.debug_mode:
        print(f"    [桥联检查] ✓ 合理 (分数: {bridge_validity['relevance_score']})")
        print(f"    [原因] {bridge_validity['reason']}")
        
except Exception as e:
    if self.debug_mode:
        print(f"    [桥联检查] 异常: {e}")
    # 检查失败时，保守策略：继续执行（不阻断流程）
    pass
# ==========================================

# (4) 检查重复
try:
    duplicate = await self.check_info_cover(...)
    # ... 后续代码 ...
```

### ✅ 修改步骤

1. 打开 `prompts_final.py`，在末尾添加 `check_bridge_validity` 模板
2. 打开 `agent_final.py`，添加 `check_bridge_validity` 方法
3. 在 `generate` 方法的SELECT操作中，构建link_qa之后添加检查逻辑
4. 保存文件

### 📊 预期效果

- 不合理的桥联被过滤（如：沟道vs栅极）
- 调试模式下可以看到桥联检查结果
- 提升多跳问题的合理性

---

## 修复4：全局JSON解析容错

### 🔧 问题描述

除了多跳生成，其他地方的JSON解析也可能失败，需要统一增强容错性。

### 📝 修复方案

#### 步骤1：添加统一的JSON解析方法

**文件**：`agent_final.py`

**位置**：在类的开头添加（约第120行，在其他方法之前）

```python
    def _safe_json_parse(self, text: str, debug_prefix: str = "") -> dict:
        """
        安全解析JSON，支持多种格式
        
        Args:
            text: LLM返回的文本
            debug_prefix: 调试前缀（用于日志）
        
        Returns:
            解析后的dict，失败返回None
        """
        result = None
        
        # 方法1: 直接解析
        try:
            result = json.loads(text)
            return result
        except json.JSONDecodeError:
            pass
        
        # 方法2: 提取```json```代码块
        try:
            if '```json' in text:
                json_text = text.split('```json')[1].split('```')[0].strip()
                result = json.loads(json_text)
                return result
        except:
            pass
        
        # 方法3: 正则提取第一个完整JSON对象
        try:
            import re
            # 匹配最外层的{}，支持嵌套
            json_match = re.search(
                r'\{(?:[^{}]|\{(?:[^{}]|\{[^{}]*\})*\})*\}', 
                text, 
                re.DOTALL
            )
            if json_match:
                result = json.loads(json_match.group())
                return result
        except:
            pass
        
        # 方法4: 提取<answer>标签内容（针对direct_generate）
        if '<answer>' in text and '</answer>' in text:
            try:
                answer = text.split('<answer>')[1].split('</answer>')[0].strip()
                return {'answer': answer}
            except:
                pass
        
        # 所有方法都失败
        if self.debug_mode and debug_prefix:
            print(f"    [{debug_prefix}] JSON解析失败")
            print(f"    [原始输出前500字符] {text[:500]}")
        
        return None
```

#### 步骤2：替换各处的JSON解析

需要修改的方法和位置：

##### 2.1 construct_base_qa（约第208行）

**修改前**：
```python
result = json.loads(text.split('```json')[1].split('```')[0].strip())
```

**修改后**：
```python
result = self._safe_json_parse(text, debug_prefix="构建基础QA")
if result is None:
    raise ValueError("基础QA JSON解析失败")
```

##### 2.2 construct_link_qa（约第248行）

**修改前**：
```python
result = json.loads(text.split('```json')[1].split('```')[0].strip())
```

**修改后**：
```python
result = self._safe_json_parse(text, debug_prefix="构建链接QA")
if result is None:
    raise ValueError("链接QA JSON解析失败")
```

##### 2.3 evaluate_question（约第330行）

**修改前**：
```python
result = json.loads(text)
```

**修改后**：
```python
result = self._safe_json_parse(text, debug_prefix="问题评估")
if result is None or 'judgement' not in result:
    return {'pass': False, 'reason': 'JSON解析失败'}
```

##### 2.4 regenerate_answer（约第370行）

**修改前**：
```python
result = json.loads(text)
```

**修改后**：
```python
result = self._safe_json_parse(text, debug_prefix="答案重生成")
if result is None or 'answer' not in result:
    raise ValueError("答案重生成JSON解析失败")
```

##### 2.5 check_qa_valid（约第400行）

**修改前**：
```python
result = json.loads(text)
```

**修改后**：
```python
result = self._safe_json_parse(text, debug_prefix="QA有效性检查")
if result is None:
    # 保守策略：解析失败时默认通过
    return True
```

##### 2.6 extract_qa_info（约第150行）

**修改前**：
```python
concepts_list = json.loads(text.split('```json')[1].split('```')[0].strip())
```

**修改后**：
```python
concepts_result = self._safe_json_parse(text, debug_prefix="提取关键概念")
if concepts_result:
    if isinstance(concepts_result, list):
        concepts_list = concepts_result
    else:
        concepts_list = []
else:
    concepts_list = []
```

### ✅ 修改步骤

1. 打开 `agent_final.py`
2. 在类的开头（第120行左右）添加 `_safe_json_parse` 方法
3. 逐个查找上述6个方法，替换JSON解析代码
4. 保存文件

### 📊 预期效果

- 系统稳定性大幅提升
- JSON解析失败率降低 **70%+**
- 调试模式下能看到详细的解析过程

---

## 可选优化：语义embedding

### 🚀 长期改进方案

使用语义embedding替代简单关键词匹配，提升相关QA查找的准确性。

### 📝 实施步骤

#### 步骤1：安装依赖

在 `requirements.txt` 中添加：

```
sentence-transformers>=2.2.0
scikit-learn>=1.0.0
```

安装：
```bash
pip install sentence-transformers scikit-learn
```

#### 步骤2：修改知识库

**文件**：`knowledge_base.py`

##### 2.1 添加导入

在文件开头添加：

```python
from sentence_transformers import SentenceTransformer
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity
```

##### 2.2 修改__init__方法

找到 `__init__` 方法，添加embedding相关初始化：

```python
def __init__(self, qa_data: list, use_embedding: bool = False):
    """
    Args:
        qa_data: QA数据列表
        use_embedding: 是否使用语义embedding查找相关QA
    """
    # ... 原有初始化代码 ...
    
    # ========== 语义embedding相关 ==========
    self.use_embedding = use_embedding
    self.embedding_model = None
    self.qa_embeddings = None
    self.qa_id_to_idx = {}  # QA-ID到索引的映射
    
    if self.use_embedding:
        print("[INFO] 加载embedding模型...")
        self.embedding_model = SentenceTransformer('all-MiniLM-L6-v2')
        self._build_embeddings()
        print("[INFO] Embedding构建完成")
```

##### 2.3 添加embedding构建方法

```python
def _build_embeddings(self):
    """构建QA的embedding向量"""
    qa_texts = []
    qa_ids = []
    
    for qa_id, qa in self.qas.items():
        text = qa.get('question', '') + ' ' + qa.get('answer', '')
        qa_texts.append(text)
        qa_ids.append(qa_id)
    
    # 构建embedding
    self.qa_embeddings = self.embedding_model.encode(
        qa_texts, 
        show_progress_bar=True,
        batch_size=32
    )
    
    # 建立ID到索引的映射
    for idx, qa_id in enumerate(qa_ids):
        self.qa_id_to_idx[qa_id] = idx
    
    print(f"[INFO] 构建了 {len(self.qa_embeddings)} 个QA的embedding")
```

##### 2.4 修改find_related_qas_prioritized

```python
def find_related_qas_prioritized(self, qa_id, top_k=5, stage='early'):
    """基于概念相似度或语义embedding找相关QA"""
    
    if self.use_embedding and self.qa_embeddings is not None:
        # 使用语义embedding
        return self._find_related_by_embedding(qa_id, top_k, stage)
    else:
        # 使用原有的关键词匹配
        return self._find_related_by_keywords(qa_id, top_k, stage)
```

##### 2.5 添加embedding查找方法

```python
def _find_related_by_embedding(self, qa_id, top_k, stage):
    """基于语义embedding查找相关QA"""
    if qa_id not in self.qa_id_to_idx:
        return []
    
    qa_idx = self.qa_id_to_idx[qa_id]
    
    # 计算相似度
    query_embedding = self.qa_embeddings[qa_idx].reshape(1, -1)
    similarities = cosine_similarity(query_embedding, self.qa_embeddings)[0]
    
    # 排序（排除自己）
    similar_indices = np.argsort(similarities)[::-1]
    
    results = []
    for idx in similar_indices:
        if idx != qa_idx:
            # 根据索引找到QA-ID
            target_qa_id = None
            for qid, qidx in self.qa_id_to_idx.items():
                if qidx == idx:
                    target_qa_id = qid
                    break
            
            if target_qa_id and target_qa_id in self.qas:
                similarity_score = float(similarities[idx])
                results.append({
                    'qa_id': target_qa_id,
                    'score': similarity_score
                })
        
        if len(results) >= top_k * 2:  # 获取2倍候选，用于动态规划
            break
    
    # 应用动态规划调整
    adjusted_results = self._apply_dynamic_planning(results, stage)
    
    return adjusted_results[:top_k]

def _find_related_by_keywords(self, qa_id, top_k, stage):
    """基于关键词匹配查找相关QA（原有逻辑）"""
    # 将原来的 find_related_qas_prioritized 的主体逻辑放到这里
    concepts = self.qa_to_concepts.get(qa_id, [])
    scores = defaultdict(int)
    
    for concept in concepts:
        related_qas = self.concept_to_qas.get(concept, [])
        for related_qa_id in related_qas:
            if related_qa_id != qa_id:
                scores[related_qa_id] += 1
    
    # ... 原有的动态规划逻辑 ...
```

#### 步骤3：修改main启用embedding

**文件**：`main_final.py`

在参数解析中添加：

```python
parser.add_argument('--use-embedding', action='store_true',
                    help='使用语义embedding查找相关QA（需要安装sentence-transformers）')
```

在初始化知识库时：

```python
# 修改前
kb = EnhancedSemiconductorKB(qa_data)

# 修改后
kb = EnhancedSemiconductorKB(qa_data, use_embedding=args.use_embedding)
```

### 🎮 使用方式

```bash
# 不使用embedding（原有方式，快速）
python main_final.py --qa-data qa_data.json --num-qa 10

# 使用embedding（新方式，更准确但较慢）
python main_final.py --qa-data qa_data.json --num-qa 10 --use-embedding
```

### 📊 预期效果

- 相关QA查找准确率提升 **30-50%**
- 语义相关性更好（不局限于关键词匹配）
- 首次运行时需要构建embedding（约1-2分钟）
- 后续运行速度正常

---

## 修复优先级建议

### 🔴 必须修复（影响功能）

| 修复 | 问题 | 影响 | 工作量 |
|-----|------|------|--------|
| **修复1** | 多跳JSON解析失败 | SELECT完全失败 | 中等 |
| **修复4** | 全局JSON解析容错 | 系统稳定性差 | 中等 |

**建议优先完成修复1和修复4**

### 🟡 强烈建议修复（影响质量）

| 修复 | 问题 | 影响 | 工作量 |
|-----|------|------|--------|
| **修复2** | 测试0/4通过 | 生成QA质量差 | 简单 |
| **修复3A** | 桥联不合理 | 多跳问题质量差 | 中等 |

**建议在修复1、4之后立即完成**

### 🟢 可选优化（长期改进）

| 优化 | 收益 | 成本 | 工作量 |
|-----|-----|------|--------|
| **修复3B** | 领域过滤 | 中等 | 较小 | 中等 |
| **embedding** | 相关性提升 | 高 | 需要额外依赖 | 较大 |

**建议在核心修复完成后，根据需求选择性实施**

---

## 测试验证

### 测试1：JSON解析成功率

```bash
# 运行生成，观察JSON解析情况
python main_final.py --qa-data qa_data.json --num-qa 10 --debug 2>&1 | grep "解析"
```

**期望输出**：
```
[解析] 方法1成功（直接解析）
[解析] 方法2成功（提取代码块）
[解析] ✓ 成功，包含所有必需字段
```

**不应出现**：
```
[多跳组合] 失败: Expecting value...
```

### 测试2：QA质量和测试通过率

```bash
# 运行生成，观察测试通过率
python main_final.py --qa-data qa_data.json --num-qa 5 --debug 2>&1 | grep "TEST"
```

**期望输出**：
```
[TEST] 正确率: 2/4  （或更高）
[TEST] 正确率: 3/4
```

**不应出现**：
```
[TEST] 正确率: 0/4
```

### 测试3：桥联合理性

```bash
# 运行生成，观察桥联检查
python main_final.py --qa-data qa_data.json --num-qa 10 --debug 2>&1 | grep "桥联检查"
```

**期望输出**：
```
[桥联检查] ✓ 合理 (分数: 8)
[桥联检查] ✗ 不合理 (分数: 3)  ← 不合理的被过滤
```

### 测试4：生成的QA

查看生成的QA文件：

```bash
# 查看最新生成的QA
ls -lt generated_qa/*.json | head -5
cat generated_qa/xxx.json | jq '.question, .answer, .num_hops'
```

**检查**：
- 问题表述是否清晰
- 是否有多跳问题（num_hops > 1）
- 答案是否完整

### 测试5：完整运行

```bash
# 完整运行，生成20个QA
python main_final.py \
    --qa-data qa_data.json \
    --num-qa 20 \
    --max-hops 3 \
    --debug \
    2>&1 | tee test_output.log

# 统计成功率
grep "DONE" test_output.log | wc -l
grep "ERROR" test_output.log | wc -l
```

---

## 修复实施建议

### 推荐顺序

```
第1步：修复4（全局JSON容错）
  ├─ 添加 _safe_json_parse 方法
  └─ 替换6处JSON解析调用
  
第2步：修复1（多跳JSON解析）
  └─ 增强 generate_multihop_question 的解析逻辑
  
第3步：修复2（基础QA质量）
  └─ 更新 base_qa prompt模板
  
第4步：修复3A（桥联检查）
  ├─ 添加 check_bridge_validity prompt
  ├─ 添加 check_bridge_validity 方法
  └─ 在SELECT中添加检查逻辑
  
第5步：（可选）embedding优化
  └─ 仅当需要进一步提升准确性时实施
```

### 时间估算

- **修复4**：30分钟
- **修复1**：15分钟
- **修复2**：10分钟
- **修复3A**：45分钟
- **总计**：约2小时

### 注意事项

1. **备份文件**
   ```bash
   cp agent_final.py agent_final.py.backup
   cp prompts_final.py prompts_final.py.backup
   ```

2. **逐个修复，逐个测试**
   - 不要一次性修改所有文件
   - 每完成一个修复，立即测试

3. **保留调试日志**
   ```bash
   python main_final.py --debug 2>&1 | tee debug.log
   ```

4. **遇到问题时**
   - 查看调试日志
   - 检查JSON格式
   - 查看LLM原始输出

---

## 总结

### 核心问题

| 问题 | 严重性 | 影响范围 | 修复难度 |
|------|--------|---------|---------|
| 多跳JSON解析失败 | ❌ 严重 | SELECT功能 | 中等 |
| 测试0/4通过 | ❌ 严重 | QA质量 | 简单 |
| 相关QA选择不合理 | ⚠️ 中等 | 多跳质量 | 中等 |
| 系统稳定性 | ⚠️ 中等 | 全局 | 中等 |

### 预期收益

完成所有修复后：

- ✅ JSON解析成功率：**20%** → **95%+**
- ✅ 测试通过率：**0/4** → **2-3/4**
- ✅ 桥联合理性：过滤不合理组合
- ✅ 系统稳定性：显著提升

### 下一步行动

1. 阅读本文档，理解各个修复方案
2. 备份当前代码
3. 按推荐顺序逐个实施修复
4. 每完成一个修复，立即测试
5. 完成后运行完整测试，验证效果

---

**修复文档创建完成！** 🎉

有任何问题，请参考文档对应章节，或查看调试日志。
