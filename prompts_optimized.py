"""
半导体QA生成系统 - 优化版Prompt模板
强调：基于子QA，不发散，垂直领域
"""


class SemiconductorQAPrompts:
    """半导体领域QA构建Prompts（优化版）"""
    
    # ========== 基础QA构建（保留原版）==========
    
    base_qa = '''你是一个半导体领域的QA构建专家。基于给定的半导体知识材料，提出一个简单但需要专业知识才能回答的问题。确保问题清晰、可解、答案唯一。

# 半导体知识材料
{content}

输出JSON格式：
```json
{{
    "question": "提出的问题",
    "answer": "问题答案",
    "statement": "该问答对涉及的核心事实陈述",
    "key_concepts": ["关键概念1", "关键概念2"]
}}
```
'''

    # ========== 关联QA构建（保留原版）==========
    
    link_qa = '''你是一个半导体领域的QA构建专家。给定两个半导体概念的信息，构建一个问题，其中答案是{conceptA}，问题上下文涉及{conceptB}。确保问题需要专业的半导体知识。

# 概念A ({conceptA}) 的信息：
```txt
{contentA}
```

# 概念B ({conceptB}) 的信息：
```txt
{contentB}
```

输出JSON格式：
```json
{{
    "question": "提出的问题",
    "answer": "问题答案",
    "statement": "连接两个概念的核心事实",
    "key_concepts": ["涉及的关键概念"]
}}
```
'''

    # ========== 🔧 优化：组合QA（使用思维链，生成参考答案）==========
    
    compose_qa = '''你是一个半导体领域的QA构建专家。将两个问题组合成一个更复杂的问题。

第一个问题：
```
{questionA}
```

第二个问题：
```
{questionB}
```

相关的技术陈述：
```
{statements}
```

---

<think>
我需要：
1. 理解两个问题之间的逻辑关系
2. 识别第二个问题的答案如何关联到第一个问题
3. 设计组合策略：隐藏第二个问题的答案，作为推理条件
4. 保持第一个问题的答案不变
5. 确保组合后的问题自然、连贯
6. 体现多步推理链条
</think>

## 【核心要求】

### 组合策略：
- ✓ 第二个问题的答案关联到第一个问题中的某个实体
- ✓ 组合后应移除第二个问题答案的直接信息
- ✓ 保持第一个问题的答案不变
- ✓ 组合后的问题应自然、连贯、专业
- ✓ 体现完整的推理链：通过问题B的内容 → 推理到问题A的答案
- ✗ 禁止简单拼接
- ✗ 禁止使用"基于"、"根据"等依赖性表述

### 问题设计：
- ✓ 单一问题，不包含多个疑问点
- ✓ 具有通用性，不特指论文
- ✓ 体现推理必要性
- ✗ 禁止复合问题

### 答案要求（⚠️ 这是参考答案，后续会重新生成）：
- ✓ 与第一个问题的答案保持一致
- ✓ 简要说明（50字内）
- ⚠️ 注意：这只是参考答案，最终答案会基于完整推理链重新生成

---

输出JSON格式：
```json
{{
    "question": "组合后的问题（单一、自然、更复杂）",
    "answer": "参考答案（与第一个问题相同，50字内）",
    "note": "如何组合的简要说明（20字内）",
    "reasoning_hint": "推理提示：需要先理解什么，再推导到什么（30字内）"
}}
```
'''

    # ========== 🆕 问题评估模板（在SELECT后立即执行）==========
    
    question_evaluation = """您是一位专家评估员，负责决定组合后的问题是否符合推理问题标准。

## 【评估标准】（严格按以下6个标准判断）：

###1. 因果性：
问题应展现出完整的技术逻辑链。比如："机制 A 怎样影响参数 B，最终致使现象 C 出现"

###2. 周密性：
思维过程要科学且严谨，需逐步思考。

###3. 可追溯性（严格执行）：
(1) 问题必须源于给定的子问答对内容
(2) 答案必须能从子问答对中推导得出
(3) 不能引入子问答对中没有的信息

###4. 问题通用性（严格执行）：
(1) 问题必须具有通用性，不局限于特定论文
(2) 禁止使用"本文"、"本研究"、"本实验"等自指表述
(3) 不能引用论文文献或文章自定义的专有名词
(4) 确保不读论文也能理解问题含义

示例：
    ❌ 错误："在金属氧化物薄膜晶体管(MOTFT)中，Nd掺杂Al合金栅极材料经300℃热处理后的电阻率具体数值是多少μΩ·cm？"
    原因：特指论文，不同材料数值不一样
    
    ✓ 正确："如何在室温溅射工艺中精确调控氧分压参数与后退火温度条件，以同步实现氧化物薄膜晶体管的高迁移率特性与长期稳定性？"
    原因：具有通用性，不特指论文

###5. 完整性：
(1) 问题全面涵盖相关内容的各个方面
(2) 问题描述简洁凝练，语义完整
(3) 问题自足，不依赖外部文章也能被清晰理解
(4) 问题能基于子问答对内容回答，但没有特指论文

###6. 单一性（必须严格执行）：
每个问题只包含一个问题，禁止连接多个子问题或在一个句子中包含多个疑问点

示例：
    ✓ 正确（单一问题）：
        "在柔性电子纸显示器驱动电路中，金属氧化物薄膜晶体管的载流子迁移率典型范围是多少？"
        "如何解决铟镓锌氧化物薄膜晶体管在光照和偏压应力下的阈值电压漂移问题？"
    
    ❌ 错误（复合问题）：
        "在柔性电子纸显示器驱动应用中，金属氧化物薄膜晶体管相较于有机薄膜晶体管，其迁移率优势如何通过载流子传输机制差异解释？并分析该优势对电子纸双稳态特性和像素响应时间的具体影响。"

---

[子问答对内容]
{sub_qa_content}

[待评估的组合问题]
{question_to_evaluate}

---

评判要求：以上6个标准全部通过才输出【是】，只要有一个不通过就是【否】。

格式要求：有且仅输出【是】或者【否】，不输出任何别的内容。严格按照以下格式输出：【是】或者【否】（严格执行！！）
"""

    # ========== 🆕 答案重新生成（基于子QA，不发散）==========
    
    answer_regeneration = """您是一位半导体领域专家，负责为组合问题生成准确、完整的最终答案。

⚠️ **核心要求：严格基于给定的子问答对，不要发散，不要引入外部知识！**

# 组合问题
{question}

# 参考答案（组合时生成的简要答案）
{reference_answer}

# 子问答对（⚠️ 你的答案必须基于这些内容）
{sub_qa_pairs}

# 技术陈述（支撑推理的关键事实）
{statements}

---

<think>
我需要：
1. **仔细阅读所有子问答对的内容**
2. **识别问题需要的推理链条**（通常是：子QA-2的内容 → 子QA-1的答案）
3. **逐步构建推理过程**，确保每一步都有子QA支撑
4. **生成完整答案**，体现推理链，但答案要自然、连贯
5. **检查答案**：是否脱离了子QA的内容？如果是，必须删除！
</think>

## 【答案生成准则】（严格执行）

### 1. 基于子QA，不发散（⚠️ 最重要）
- ✓ 答案的每一句话都必须有子问答对的支撑
- ✓ 推理过程必须基于子问答对中的信息
- ✓ 不得引入子问答对中没有提到的概念、数据、结论
- ✓ 如果子问答对信息不足，宁可答案简短，也不要编造
- ✗ 禁止使用子问答对外的专业知识
- ✗ 禁止发散到相关但未提及的技术点
- ✗ 禁止引入"可能"、"通常"等推测性表述（除非子QA中有）

### 2. 完整性
- ✓ 答案必须完整回答问题的所有方面
- ✓ 体现推理过程（但要自然，不要生硬列举步骤）
- ✓ 答案长度：80-200字
- ✗ 禁止仅引导句
- ✗ 禁止只回答部分问题

### 3. 推理链体现
- ✓ 答案应自然地体现：通过理解A → 推导到B
- ✓ 使用逻辑连接词："由于...因此..."、"通过...可以..."
- ✓ 推理步骤要清晰，但表达要流畅
- ✗ 禁止机械地列举"第一步、第二步"

### 4. 通用性
- ✓ 答案具有通用性，不特指论文
- ✓ 使用通用的技术术语
- ✗ 禁止使用"本文"、"本研究"等表述
- ✗ 禁止引用文献

### 5. 准确性验证
- ✓ 生成答案后，逐句检查：这句话在子QA中有依据吗？
- ✓ 如果没有依据，必须删除
- ✓ 最终答案的所有信息都能在子QA中找到来源

## 【答案策略】

根据问题类型选择策略：
- **因果推理**：体现 原因 → 机制 → 结果
- **参数影响**：体现 参数变化 → 性能影响 → 最终效果
- **工艺优化**：体现 工艺条件 → 材料特性 → 器件性能

## 【禁止事项】

× 禁止引入子问答对中没有的信息
× 禁止使用"可能"、"通常"、"一般来说"等推测（除非子QA中有）
× 禁止发散到相关但未提及的技术
× 禁止使用"本文/本研究/本实验"等论文自指
× 禁止答案不完整（仅引导句）
× 禁止过度发散（垂直领域模型容易引入错误）

---

输出JSON格式：
```json
{{
    "final_answer": "最终生成的完整答案（80-200字，严格基于子QA，体现推理链）",
    "reasoning_trace": "推理轨迹（30-50字，说明如何从子QA推导到答案）",
    "confidence": 0.0-1.0,
    "grounded_check": {{
        "all_info_from_subqa": true/false,
        "no_external_knowledge": true/false,
        "complete_reasoning": true/false
    }}
}}
```
"""

    # ========== Action选择（完全保留原版）==========
    
    action = '''你是一个半导体领域的QA构建专家。基于当前问答对和相关信息，选择一个操作来提升问题难度。

# 当前问答对
{question}

# 可选操作
{actions}

输出JSON格式（根据选择的操作）：
见下方各操作的详细格式
'''

    SELECT = '''SELECT: 从相关概念列表中选择一个概念，融入问题以增加推理深度。

输出JSON格式：
```json
{{
    "action": "SELECT",
    "target": "选中概念的ID（必须来自相关概念列表）",
    "note": "选择该概念的理由（说明如何增加推理深度，20-40字）"
}}
```
'''

    FUZZ = '''FUZZ: 模糊化问题中的1处信息，使问题更具挑战性，需要更多推理。

输出JSON格式：
```json
{{
    "action": "FUZZ",
    "question": "模糊化后的问题（清晰、可解、更具挑战）",
    "note": "为何以及如何进行模糊化（20-40字）"
}}
```
'''

    EXIT = '''EXIT: 当问题满足退出条件时选择退出。

输出JSON格式：
```json
{{
    "action": "EXIT",
    "note": "为何选择退出（说明问题已满足质量标准，20-40字）"
}}
```
'''

    BRAINSTORM = '''BRAINSTORM: 头脑风暴与当前问答对相关的半导体概念，用于后续融入问题。

输出JSON格式：
```json
{{
    "action": "BRAINSTORM",
    "entities": [
        {{
            "name": "概念名称（准确、专业）",
            "concept_id": "概念在知识库中的ID（如果有，否则为null）",
            "statement": "连接该概念与当前问答的事实陈述（20-50字）"
        }}
    ]
}}
```
'''

    # ========== 辅助Prompts（保留原版）==========
    
    extract_key_concepts = '''从以下半导体知识内容中提取关键技术概念。

内容：
```txt
{content}
```

输出JSON格式：
```json
[
    {{"concept": "概念名", "type": "材料/工艺/器件/效应/参数"}},
    {{"concept": "概念名", "type": "材料/工艺/器件/效应/参数"}}
]
```
'''

    summarize_qa = '''总结以下半导体问答对的核心技术要点。

问答对：
```txt
问题: {question}
答案: {answer}
```

输出格式：
<summary>核心技术摘要</summary>
<key_points>
- 关键点1
- 关键点2
</key_points>
'''

    check_info_cover = '''检查新陈述的信息是否已被先前陈述完全覆盖。

先前的技术陈述：
```txt
{prior}
```

待检查的陈述：
```txt
{current}
```

输出JSON格式：
```json
{{
    "judgement": "yes 或 no",
    "analysis": "简要分析（20字内）"
}}
```
'''

    check_alternative_ans = '''判断预测答案是否也是该半导体问题的正确答案。

问题: {question}

标准答案: {gt_answer}

支撑标准答案的技术事实：
```txt
{statements}
```

预测答案: {pred_answer}

输出JSON格式：
```json
{{
    "judgement": "yes 或 no",
    "reason": "判断理由（30字内）"
}}
```
'''

    qa_valid_check = '''检查该半导体问答对的有效性。

问答对有效当且仅当：
1. 问题不是简单拼接多个问题
2. 提供的答案是唯一正确答案
3. 问题有唯一答案
4. 基于相关技术陈述可以解答

问答对和相关信息：
{question}

输出JSON格式：
```json
{{
    "judgement": "yes 或 no",
    "analysis": "分析说明（50字内）"
}}
```
'''

    direct_gen_check = """回答以下半导体技术问题，将答案放在<answer></answer>标签内。

{question}

输出格式：
<answer>你的答案</answer>
"""

    llm_judge = """你是一个评估助手。判断预测答案是否等价于标准答案。

问题: {question}

标准答案: {gt_answer}

预测答案: {pred_answer}

如果等价回答"Correct"，否则回答"Incorrect"。不要包含其他文字。
"""
