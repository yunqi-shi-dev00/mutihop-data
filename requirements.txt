# 半导体QA生成系统 - 依赖项

# 核心依赖
torch>=2.0.0
transformers>=4.30.0
aiohttp>=3.8.0
requests>=2.28.0
tqdm>=4.65.0
numpy>=1.24.0

# 可选依赖（语义embedding，如果使用--use-embedding）
sentence-transformers>=2.2.0

# 开发依赖（测试）
pytest>=7.3.0
