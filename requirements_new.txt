# 半导体QA生成系统 - 依赖包列表（优化版）

# 基础依赖
asyncio
aiohttp>=3.8.0
requests>=2.28.0
numpy>=1.24.0
transformers>=4.35.0
tqdm>=4.65.0

# 推理框架（二选一）
# vLLM - 推荐：更成熟稳定
vllm>=0.2.0

# 或者使用 SGLang - 更快，但文档较少
# sglang[all]

# 🚀 新增：语义embedding支持（可选）
# 如果使用 --use-embedding 参数，需要安装以下依赖
sentence-transformers>=2.2.0
scikit-learn>=1.0.0

# 其他工具
pathlib
