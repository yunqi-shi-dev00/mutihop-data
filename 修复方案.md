# 修复方案 🔧

## 目录
1. [修复1：多跳JSON解析失败](#修复1多跳json解析失败)
2. [修复2：测试0/4通过（问题表述改进）](#修复2测试04通过问题表述改进)
3. [修复3：相关QA选择不合理](#修复3相关qa选择不合理)
4. [修复4：增强JSON解析容错性](#修复4增强json解析容错性)
5. [可选优化：使用语义embedding](#可选优化使用语义embedding)

---

## 修复1：多跳JSON解析失败

### 问题描述
```
[多跳组合] 失败: Expecting value: line 7 column 3 (char 338)
```

LLM返回的JSON格式不正确，导致`json.loads()`失败。

### 修复方案

**文件**：`agent_final.py`

**位置**：`generate_multihop_question` 方法（约第265-295行）

#### 修改前

```python
async def generate_multihop_question(self, memory):
    """生成多跳问题"""
    num_hops = len(memory.relevant)
    if num_hops < 2:
        return None
    
    # ... 构建prompt ...
    
    text = await self.call_llm(prompt, temperature=0.7)
    
    # 直接解析JSON - 容易失败
    result = json.loads(text)
    
    return {
        'question': result['question'],
        'answer': result['answer'],
        'reasoning_steps': result.get('reasoning_steps', []),
        # ...
    }
```

#### 修改后

```python
async def generate_multihop_question(self, memory):
    """生成多跳问题"""
    num_hops = len(memory.relevant)
    if num_hops < 2:
        return None
    
    # ... 构建prompt ...
    
    text = await self.call_llm(prompt, temperature=0.7)
    
    # ===== 增强JSON解析 =====
    result = None
    
    # 方法1: 直接解析
    try:
        result = json.loads(text)
    except json.JSONDecodeError as e:
        if self.debug_mode:
            print(f"    [解析] 方法1失败: {e}")
        
        # 方法2: 提取```json```代码块
        try:
            if '```json' in text:
                json_text = text.split('```json')[1].split('```')[0].strip()
                result = json.loads(json_text)
                if self.debug_mode:
                    print(f"    [解析] 方法2成功（提取代码块）")
            else:
                raise ValueError("没有找到```json```标记")
        except Exception as e2:
            if self.debug_mode:
                print(f"    [解析] 方法2失败: {e2}")
            
            # 方法3: 正则提取第一个完整JSON对象
            try:
                import re
                # 匹配最外层的{}
                json_match = re.search(r'\{(?:[^{}]|\{(?:[^{}]|\{[^{}]*\})*\})*\}', text, re.DOTALL)
                if json_match:
                    result = json.loads(json_match.group())
                    if self.debug_mode:
                        print(f"    [解析] 方法3成功（正则提取）")
                else:
                    raise ValueError("正则未匹配到JSON对象")
            except Exception as e3:
                if self.debug_mode:
                    print(f"    [解析] 方法3失败: {e3}")
                    print(f"    [原始输出] {text[:500]}...")
                return None
    
    if result is None:
        return None
    
    # 验证必需字段
    required_fields = ['question', 'answer', 'reasoning_steps']
    if not all(field in result for field in required_fields):
        if self.debug_mode:
            print(f"    [解析] 缺少必需字段，当前字段: {list(result.keys())}")
        return None
    
    return {
        'question': result['question'],
        'answer': result['answer'],
        'reasoning_steps': result.get('reasoning_steps', []),
        'quality_indicators': result.get('quality_indicators', {}),
        'metadata': result.get('metadata', {})
    }
```

### 修改步骤

1. 找到`agent_final.py`的`generate_multihop_question`方法
2. 定位到`text = await self.call_llm(...)`之后的JSON解析部分
3. 替换为上述增强版解析代码
4. 保存文件

---

## 修复2：测试0/4通过（问题表述改进）

### 问题描述

原始问题表述有歧义，导致LLM无法正确回答：

```
问题: "如果氧化铟的含量达到80%，氧化铟的最大含量应该不超过多少？"
LLM理解: "已经到80%了，那最大就是80%"
正确答案: "88%"
```

### 修复方案

**文件**：`prompts_final.py`

**位置**：`base_qa` 模板（约第14-38行）

#### 修改前

```python
base_qa = '''你是一个半导体领域的多跳QA构建专家。基于给定的半导体知识材料，对问答对语法优化。确保问题清晰、可解、答案唯一。

# 半导体知识材料
{qa_entity}

输出JSON格式：
```json
{{
    "question": "针对原问题语法改善后的问题",
    "answer": "针对原始答案语法改善后的问题答案",
    "statement": "该问答对涉及的核心事实陈述",
    "key_concepts": ["关键概念1", "关键概念2"]
}}
```
'''
```

#### 修改后

```python
base_qa = '''你是一个半导体领域的多跳QA构建专家。基于给定的半导体知识材料，对问答对语法优化。确保问题清晰、可解、答案唯一。

# 半导体知识材料
{qa_entity}

## 【核心要求】

### 1. 问题改写原则（严格执行）：

**(1) 清晰性**
- 问题表述必须明确、无歧义
- 避免使用"如果...达到X%，最大是多少"这类容易产生歧义的表述
- 如果原问题涉及"阈值、上限、范围"，应直接询问具体数值

**(2) 可解性**
- 问题必须基于给定答案可直接回答
- 不引入答案中没有的信息
- 确保问答逻辑一致

**(3) 答案唯一性**
- 问题应有唯一正确答案
- 避免模糊的多解释空间

### 2. 常见问题改写示例：

**错误示例1（有歧义）**：
```
原问题: "当氧化铟含量达到80%时，为维持非晶结构，氧化铟含量上限应控制在多少？"
错误改写: "如果氧化铟的含量达到80%，氧化铟的最大含量应该不超过多少？"
               ↑ 容易理解成"已经到80%了，最大就是80%"

正确改写: "在IGZO薄膜晶体管中，为维持非晶结构特性，氧化铟含量的上限阈值是多少？"
               ↑ 直接询问上限，清晰无歧义
```

**错误示例2（有歧义）**：
```
原问题: "温度从200°C开始，最高可加热到多少？"
错误改写: "如果温度达到200°C，最高温度应该不超过多少？"
               ↑ 容易理解成"最高就是200°C"

正确改写: "退火工艺中，温度的上限阈值是多少？"
               ↑ 直接询问上限
```

**正确示例**：
```
原问题: "IGZO薄膜中ZnO含量对透过率的影响？"
改写: "在IGZO薄膜材料中，ZnO含量如何影响可见光透过率？"
        ↑ 保持原意，语法更清晰
```

### 3. 改写检查清单：

在输出前，自我检查：
- [ ] 问题是否直接、明确？
- [ ] 是否避免了"如果...达到X，最大是多少"这类表述？
- [ ] 如果涉及阈值/上限/范围，是否直接询问数值？
- [ ] LLM看到这个问题能直接给出正确答案吗？
- [ ] 答案和问题是否逻辑一致？

输出JSON格式：
```json
{{
    "question": "针对原问题语法改善后的问题（清晰、无歧义、可直接回答）",
    "answer": "针对原始答案语法改善后的问题答案",
    "statement": "该问答对涉及的核心事实陈述",
    "key_concepts": ["关键概念1", "关键概念2"]
}}
```
'''
```

### 修改步骤

1. 打开`prompts_final.py`
2. 找到`base_qa`模板定义
3. 替换为上述增强版模板
4. 保存文件

---

## 修复3：相关QA选择不合理

### 问题描述

使用简单的关键词匹配选择相关QA，导致：
- QA-6（IGZO沟道材料）和 QA-15（铝栅极材料）被强行桥联
- 两者技术领域不同，桥联不合理

### 修复方案A：添加桥联合理性检查

**文件**：`agent_final.py`

**位置**：`generate`方法中的SELECT操作（约第600行）

#### 在构建link_qa之后添加合理性检查

```python
# (3) 构建link_qa
try:
    link_qa = await self.construct_link_qa(target, neighbor_entity)
except Exception as e:
    print(f"[WARNING] 构建link_qa失败: {e}")
    continue

# ===== 新增：桥联合理性检查 =====
try:
    bridge_validity = await self.check_bridge_validity(
        target, 
        neighbor_entity, 
        link_qa['statement']
    )
    
    if not bridge_validity['is_valid']:
        if self.debug_mode:
            print(f"    [桥联检查] ✗ 不合理: {bridge_validity['reason']}")
        continue
    
    if self.debug_mode:
        print(f"    [桥联检查] ✓ 合理: {bridge_validity['reason']}")
        
except Exception as e:
    if self.debug_mode:
        print(f"    [桥联检查] 异常: {e}")
    # 检查失败时，保守策略：继续执行
    pass

# (4) 检查重复
try:
    duplicate = await self.check_info_cover(...)
    # ...
```

#### 添加新方法：check_bridge_validity

在`agent_final.py`的类中添加新方法（建议放在第700行左右）：

```python
async def check_bridge_validity(self, qa1: SemiconductorQAEntity, qa2: SemiconductorQAEntity, statement: str) -> dict:
    """检查两个QA之间的桥联是否合理"""
    prompt = SemiconductorQAPrompts.check_bridge_validity.format(
        qa1_question=qa1.qa_data['question'],
        qa1_answer=qa1.qa_data['answer'],
        qa1_concepts=', '.join(qa1.key_concepts),
        qa2_question=qa2.qa_data['question'],
        qa2_answer=qa2.qa_data['answer'],
        qa2_concepts=', '.join(qa2.key_concepts),
        statement=statement
    )
    
    text = await self.call_llm(prompt, temperature=0.3)
    
    # 解析结果
    try:
        result = json.loads(text)
        return {
            'is_valid': result['judgement'].lower() == 'yes',
            'reason': result.get('analysis', ''),
            'relevance_score': result.get('relevance_score', 0)
        }
    except:
        # 解析失败时，简单判断
        if '【是】' in text or 'yes' in text.lower():
            return {'is_valid': True, 'reason': '格式异常但判断为是'}
        else:
            return {'is_valid': False, 'reason': '格式异常或判断为否'}
```

#### 添加新prompt模板

**文件**：`prompts_final.py`

在类中添加新模板（建议放在第520行左右）：

```python
check_bridge_validity = '''你是一个半导体领域的技术专家。判断两个QA之间的桥联是否合理。

# QA-1 信息
问题: {qa1_question}
答案: {qa1_answer}
关键概念: {qa1_concepts}

# QA-2 信息
问题: {qa2_question}
答案: {qa2_answer}
关键概念: {qa2_concepts}

# 桥联声明
{statement}

## 【判断标准】

### 合理的桥联应满足：

1. **技术相关性**
   - 两个QA涉及相同或相关的技术领域
   - 例如：都涉及TFT器件、都涉及材料制备、都涉及工艺参数

2. **逻辑连贯性**
   - 从QA-1推理到QA-2有明确的逻辑路径
   - 不是简单的表面概念重叠

3. **知识深度**
   - 桥联能够揭示更深层的技术关联
   - 不是泛泛而谈的"都涉及半导体"

### 不合理的桥联示例：

**示例1（领域不同）**：
```
QA-1: IGZO沟道材料的氧化铟含量控制
QA-2: 铝栅极材料的小丘抑制
桥联: "都涉及材料和掺杂"
判断: 不合理 - 沟道和栅极是不同的器件组件
```

**示例2（关联太弱）**：
```
QA-1: TFT的迁移率优化
QA-2: 显示器的色域标准
桥联: "都与显示技术相关"
判断: 不合理 - 器件性能和显示效果关联太远
```

### 合理的桥联示例：

**示例1（工艺链）**：
```
QA-1: IGZO薄膜的氧分压控制
QA-2: IGZO薄膜的退火温度优化
桥联: "氧分压和退火温度共同影响IGZO薄膜的氧空位浓度"
判断: 合理 - 同一材料的不同工艺参数
```

**示例2（性能关联）**：
```
QA-1: TFT的阈值电压
QA-2: TFT的迁移率
桥联: "阈值电压和迁移率共同决定TFT的开关特性"
判断: 合理 - 同一器件的相关性能参数
```

## 【评分标准】

- **相关性分数**（0-10分）：
  - 9-10分：强相关（同一器件/材料/工艺的不同方面）
  - 6-8分：中等相关（相关技术领域）
  - 3-5分：弱相关（表面概念重叠）
  - 0-2分：不相关（不同技术领域）

- **合格线**：相关性分数 ≥ 6分

## 【输出格式】

```json
{{
    "judgement": "yes 或 no",
    "analysis": "分析说明（50字内，说明为何合理或不合理）",
    "relevance_score": 0-10
}}
```
'''
```

### 修改步骤

1. 在`prompts_final.py`中添加`check_bridge_validity`模板
2. 在`agent_final.py`中添加`check_bridge_validity`方法
3. 在`generate`方法的SELECT操作中，在`construct_link_qa`之后添加合理性检查
4. 保存文件

---

### 修复方案B：改进相关QA查找算法（可选）

如果想从源头改进QA选择，修改知识库的查找逻辑。

**文件**：`knowledge_base.py`

**位置**：`find_related_qas_prioritized`方法（约第80行）

#### 修改前

```python
def find_related_qas_prioritized(self, qa_id, top_k=5, stage='early'):
    """基于概念相似度找相关QA，并根据阶段调整优先级"""
    concepts = self.qa_to_concepts.get(qa_id, [])
    scores = defaultdict(int)
    
    for concept in concepts:
        related_qas = self.concept_to_qas.get(concept, [])
        for related_qa_id in related_qas:
            if related_qa_id != qa_id:
                scores[related_qa_id] += 1  # 简单计数
    
    # 后续动态规划逻辑...
```

#### 修改后（添加领域过滤）

```python
def find_related_qas_prioritized(self, qa_id, top_k=5, stage='early'):
    """基于概念相似度找相关QA，并根据阶段调整优先级"""
    concepts = self.qa_to_concepts.get(qa_id, [])
    scores = defaultdict(int)
    
    # 获取源QA的领域信息
    source_domain = self._infer_domain(qa_id, concepts)
    
    for concept in concepts:
        related_qas = self.concept_to_qas.get(concept, [])
        for related_qa_id in related_qas:
            if related_qa_id != qa_id:
                # 基础分数：共同概念数
                base_score = 1
                
                # 领域加成：同领域+2分
                target_concepts = self.qa_to_concepts.get(related_qa_id, [])
                target_domain = self._infer_domain(related_qa_id, target_concepts)
                if source_domain == target_domain and source_domain != 'unknown':
                    base_score += 2
                
                scores[related_qa_id] += base_score
    
    # 后续动态规划逻辑...

def _infer_domain(self, qa_id, concepts):
    """推断QA所属的技术领域"""
    # 定义领域关键词
    domain_keywords = {
        'channel': ['沟道', '迁移率', 'IGZO', 'ITZO', '载流子', '电子传输', '氧空位'],
        'gate': ['栅极', '栅介质', '阈值电压', '介电常数', '漏电流'],
        'electrode': ['电极', '接触电阻', '功函数', '欧姆接触'],
        'passivation': ['钝化层', '保护层', '封装'],
        'process': ['退火', '氧分压', '沉积', '刻蚀', '溅射', '工艺'],
        'display': ['显示', '像素', 'AMOLED', 'LCD', '刷新率', '分辨率']
    }
    
    # 统计每个领域的匹配度
    domain_scores = defaultdict(int)
    
    # 从QA内容中提取文本
    qa_text = ''
    if qa_id in self.qas:
        qa_data = self.qas[qa_id]
        qa_text = qa_data.get('question', '') + ' ' + qa_data.get('answer', '')
    
    # 加上概念
    qa_text += ' ' + ' '.join(concepts)
    qa_text = qa_text.lower()
    
    # 计算匹配度
    for domain, keywords in domain_keywords.items():
        for keyword in keywords:
            if keyword in qa_text:
                domain_scores[domain] += 1
    
    # 返回得分最高的领域
    if domain_scores:
        return max(domain_scores.items(), key=lambda x: x[1])[0]
    else:
        return 'unknown'
```

### 修改步骤（方案B）

1. 打开`knowledge_base.py`
2. 在`EnhancedSemiconductorKB`类中添加`_infer_domain`方法
3. 修改`find_related_qas_prioritized`方法，加入领域过滤逻辑
4. 保存文件

---

## 修复4：增强JSON解析容错性（全局）

除了修复1针对多跳生成，还需要在其他JSON解析处增强容错。

### 位置清单

需要在以下方法中增强JSON解析：

1. `construct_base_qa`（约第190行）
2. `construct_link_qa`（约第230行）
3. `evaluate_question`（约第310行）
4. `regenerate_answer`（约第350行）
5. `check_qa_valid`（约第390行）
6. `llm_judge_answer`（约第440行）

### 统一的JSON解析函数

**文件**：`agent_final.py`

在类的开头添加通用解析函数（约第120行）：

```python
def _safe_json_parse(self, text: str, debug_prefix: str = "") -> dict:
    """安全解析JSON，支持多种格式"""
    result = None
    
    # 方法1: 直接解析
    try:
        result = json.loads(text)
        return result
    except json.JSONDecodeError:
        pass
    
    # 方法2: 提取```json```代码块
    try:
        if '```json' in text:
            json_text = text.split('```json')[1].split('```')[0].strip()
            result = json.loads(json_text)
            return result
    except:
        pass
    
    # 方法3: 正则提取第一个完整JSON对象
    try:
        import re
        json_match = re.search(r'\{(?:[^{}]|\{(?:[^{}]|\{[^{}]*\})*\})*\}', text, re.DOTALL)
        if json_match:
            result = json.loads(json_match.group())
            return result
    except:
        pass
    
    # 方法4: 提取<answer>标签内容（针对direct_generate）
    if '<answer>' in text and '</answer>' in text:
        try:
            answer = text.split('<answer>')[1].split('</answer>')[0].strip()
            return {'answer': answer}
        except:
            pass
    
    # 所有方法都失败
    if self.debug_mode and debug_prefix:
        print(f"    [{debug_prefix}] JSON解析失败")
        print(f"    [原始输出前500字符] {text[:500]}")
    
    return None
```

### 替换现有解析

然后在各个方法中，将：

```python
# 修改前
result = json.loads(text)
```

替换为：

```python
# 修改后
result = self._safe_json_parse(text, debug_prefix="方法名")
if result is None:
    return None  # 或其他错误处理
```

### 具体替换位置

#### 1. construct_base_qa（约第208行）

```python
# 修改前
result = json.loads(text.split('```json')[1].split('```')[0].strip())

# 修改后
result = self._safe_json_parse(text, debug_prefix="构建基础QA")
if result is None:
    raise ValueError("基础QA JSON解析失败")
```

#### 2. construct_link_qa（约第248行）

```python
# 修改前
result = json.loads(text.split('```json')[1].split('```')[0].strip())

# 修改后
result = self._safe_json_parse(text, debug_prefix="构建链接QA")
if result is None:
    raise ValueError("链接QA JSON解析失败")
```

#### 3. evaluate_question（约第330行）

```python
# 修改前
result = json.loads(text)

# 修改后
result = self._safe_json_parse(text, debug_prefix="问题评估")
if result is None or 'judgement' not in result:
    return {'pass': False, 'reason': 'JSON解析失败'}
```

#### 4. regenerate_answer（约第370行）

```python
# 修改前
result = json.loads(text)

# 修改后
result = self._safe_json_parse(text, debug_prefix="答案重生成")
if result is None or 'answer' not in result:
    raise ValueError("答案重生成JSON解析失败")
```

#### 5. check_qa_valid（约第400行）

```python
# 修改前
result = json.loads(text)

# 修改后
result = self._safe_json_parse(text, debug_prefix="QA有效性检查")
if result is None:
    return True  # 保守策略：解析失败时默认通过
```

#### 6. llm_judge_answer（约第450行）

```python
# 修改前
# 这个方法不需要JSON解析，已经是文本判断
```

---

## 可选优化：使用语义embedding

如果想从根本上改进相关QA的查找，可以使用语义embedding替代简单关键词匹配。

### 步骤1：安装依赖

在`requirements.txt`中添加：

```
sentence-transformers>=2.2.0
```

### 步骤2：修改知识库初始化

**文件**：`knowledge_base.py`

#### 在类的开头添加

```python
from sentence_transformers import SentenceTransformer
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity
```

#### 在__init__中添加

```python
def __init__(self, qa_data: list, use_embedding: bool = False):
    """
    Args:
        qa_data: QA数据列表
        use_embedding: 是否使用语义embedding查找相关QA
    """
    # ... 原有初始化代码 ...
    
    # 语义embedding相关
    self.use_embedding = use_embedding
    self.embedding_model = None
    self.qa_embeddings = None
    
    if self.use_embedding:
        print("[INFO] 加载embedding模型...")
        self.embedding_model = SentenceTransformer('all-MiniLM-L6-v2')
        self._build_embeddings()
        print("[INFO] Embedding构建完成")

def _build_embeddings(self):
    """构建QA的embedding向量"""
    qa_texts = []
    for qa in self.qas.values():
        text = qa.get('question', '') + ' ' + qa.get('answer', '')
        qa_texts.append(text)
    
    self.qa_embeddings = self.embedding_model.encode(qa_texts, show_progress_bar=True)
    print(f"[INFO] 构建了 {len(self.qa_embeddings)} 个QA的embedding")
```

#### 修改find_related_qas_prioritized

```python
def find_related_qas_prioritized(self, qa_id, top_k=5, stage='early'):
    """基于概念相似度或语义embedding找相关QA"""
    
    if self.use_embedding and self.qa_embeddings is not None:
        # 使用语义embedding
        return self._find_related_by_embedding(qa_id, top_k, stage)
    else:
        # 使用原有的关键词匹配
        return self._find_related_by_keywords(qa_id, top_k, stage)

def _find_related_by_embedding(self, qa_id, top_k, stage):
    """基于语义embedding查找相关QA"""
    qa_idx = int(qa_id.replace('QA-', '')) - 1  # 假设QA-1对应索引0
    
    if qa_idx < 0 or qa_idx >= len(self.qa_embeddings):
        return []
    
    # 计算相似度
    query_embedding = self.qa_embeddings[qa_idx].reshape(1, -1)
    similarities = cosine_similarity(query_embedding, self.qa_embeddings)[0]
    
    # 排序（排除自己）
    similar_indices = np.argsort(similarities)[::-1]
    
    results = []
    for idx in similar_indices:
        if idx != qa_idx:
            qa_id_str = f"QA-{idx + 1}"
            if qa_id_str in self.qas:
                similarity_score = float(similarities[idx])
                results.append({
                    'qa_id': qa_id_str,
                    'score': similarity_score
                })
        
        if len(results) >= top_k * 2:  # 获取2倍候选
            break
    
    # 应用动态规划调整
    adjusted_results = self._apply_dynamic_planning(results, stage)
    
    return adjusted_results[:top_k]

def _find_related_by_keywords(self, qa_id, top_k, stage):
    """基于关键词匹配查找相关QA（原有逻辑）"""
    # ... 原有的find_related_qas_prioritized逻辑 ...
```

### 步骤3：修改main启用embedding

**文件**：`main_final.py`

```python
# 修改前
kb = EnhancedSemiconductorKB(qa_data)

# 修改后
parser.add_argument('--use-embedding', action='store_true', 
                    help='使用语义embedding查找相关QA')

kb = EnhancedSemiconductorKB(qa_data, use_embedding=args.use_embedding)
```

### 使用方式

```bash
# 不使用embedding（原有方式）
python main_final.py --qa-data qa_data.json

# 使用embedding（新方式）
python main_final.py --qa-data qa_data.json --use-embedding
```

---

## 修复优先级建议

### 🔴 必须修复（影响功能）

1. ✅ **修复1：多跳JSON解析失败** - 导致SELECT完全失败
2. ✅ **修复4：增强JSON解析容错性** - 提升系统稳定性

### 🟡 强烈建议修复（影响质量）

3. ✅ **修复2：测试0/4通过** - 改进基础QA质量
4. ✅ **修复3A：添加桥联合理性检查** - 避免不合理的QA组合

### 🟢 可选优化（长期改进）

5. ⚪ **修复3B：改进相关QA查找** - 领域过滤
6. ⚪ **可选优化：使用语义embedding** - 需要额外依赖

---

## 测试验证

修复后，建议按以下步骤验证：

### 1. 测试JSON解析

```bash
# 运行生成，观察是否还有JSON解析失败
python main_final.py --qa-data qa_data.json --num-qa 5 --debug
```

**期望结果**：
- `[多跳组合] 失败` 大幅减少
- 如果失败，能看到详细的调试信息

### 2. 测试QA质量

观察生成的QA：

```bash
# 查看生成的QA
cat generated_qa/*.json | jq '.question, .answer'
```

**期望结果**：
- 问题表述更清晰
- 测试通过率 > 0/4

### 3. 测试桥联合理性

```bash
# 运行时观察桥联检查日志
python main_final.py --qa-data qa_data.json --num-qa 10 --debug | grep "桥联检查"
```

**期望结果**：
- 看到 `[桥联检查] ✓ 合理` 或 `[桥联检查] ✗ 不合理`
- 不合理的桥联被跳过

---

## 快速修复脚本

如果需要批量修复，可以创建一个脚本：

**文件**：`apply_fixes.sh`

```bash
#!/bin/bash

echo "=== 开始应用修复 ==="

# 备份原文件
echo "[1/5] 备份原文件..."
cp agent_final.py agent_final.py.backup
cp prompts_final.py prompts_final.py.backup
cp knowledge_base.py knowledge_base.py.backup

echo "[2/5] 修复1：多跳JSON解析..."
# 手动操作，这里仅提示
echo "    → 请手动修改 agent_final.py 的 generate_multihop_question 方法"

echo "[3/5] 修复2：基础QA prompt..."
echo "    → 请手动修改 prompts_final.py 的 base_qa 模板"

echo "[4/5] 修复3：桥联合理性检查..."
echo "    → 请手动添加 check_bridge_validity 方法和模板"

echo "[5/5] 修复4：全局JSON解析容错..."
echo "    → 请手动添加 _safe_json_parse 方法并替换各处调用"

echo ""
echo "=== 修复清单 ==="
echo "✅ agent_final.py    - 添加 _safe_json_parse 方法"
echo "✅ agent_final.py    - 修改 generate_multihop_question"
echo "✅ agent_final.py    - 添加 check_bridge_validity"
echo "✅ prompts_final.py  - 修改 base_qa 模板"
echo "✅ prompts_final.py  - 添加 check_bridge_validity 模板"
echo ""
echo "修复完成后，运行测试："
echo "  python main_final.py --qa-data qa_data.json --num-qa 5 --debug"
```

---

## 总结

按照上述修复方案，可以解决当前发现的主要问题：

| 问题 | 修复方案 | 预期效果 |
|------|---------|---------|
| 多跳JSON解析失败 | 增强解析（3种方法） | 成功率提升 80%+ |
| 测试0/4通过 | 改进base_qa prompt | 通过率提升至 ≥50% |
| 相关QA选择不合理 | 添加桥联检查 | 过滤不合理组合 |
| 系统稳定性 | 全局JSON容错 | 减少崩溃 |

**建议修复顺序**：修复1 → 修复4 → 修复2 → 修复3A → (可选) 修复3B/embedding

修复完成后，请重新运行测试并观察效果！ 🚀
