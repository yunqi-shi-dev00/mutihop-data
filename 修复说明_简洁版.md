# 修复说明 - 简洁版 🔧

## 🎯 两个核心修改

### 1️⃣ 修复错误：`sequence item 0: expected str instance, dict found`

**错误原因**：
```python
# extract_key_concepts 返回的是字典列表
entity.key_concepts = [
    {"name": "IGZO", "type": "材料"},
    {"name": "氧分压", "type": "工艺"}
]

# 但 repr() 方法中尝试：
concepts_str = ', '.join(self.key_concepts)  # ❌ 不能join字典
```

**修复位置**：`knowledge_base_new.py`

**修复1：`repr()` 方法（约第418行）**
```python
# 修改前
concepts_str = ', '.join(self.key_concepts) if self.key_concepts else '待提取'

# 修改后
if self.key_concepts:
    if isinstance(self.key_concepts[0], str):
        concepts_str = ', '.join(self.key_concepts)
    elif isinstance(self.key_concepts[0], dict):
        concepts_str = ', '.join([c.get('name', str(c)) for c in self.key_concepts])
    else:
        concepts_str = ', '.join([str(c) for c in self.key_concepts])
else:
    concepts_str = '待提取'
```

**修复2：`dict()` 方法（约第460行）**
```python
# 修改前
'key_concepts': self.key_concepts,

# 修改后
if self.key_concepts and isinstance(self.key_concepts[0], dict):
    key_concepts_str = [c.get('name', str(c)) for c in self.key_concepts]
else:
    key_concepts_str = self.key_concepts

return {
    ...
    'key_concepts': key_concepts_str,  # 使用转换后的
    ...
}
```

---

### 2️⃣ 使用本地Embedding模型

**模型路径**：`/mnt/data/LLM/hhh/qwen3_emb/backup_h/Qwen3-Embedding-0.6B_sft_v5`

**修复位置**：`knowledge_base_new.py` 的 `_build_embeddings()` 方法（约第100行）

**核心代码**：
```python
def _build_embeddings(self):
    """🚀 构建QA的embedding向量（使用本地Qwen3-Embedding模型）"""
    try:
        # 使用本地Qwen3-Embedding模型
        local_model_path = "/mnt/data/LLM/hhh/qwen3_emb/backup_h/Qwen3-Embedding-0.6B_sft_v5"
        
        print(f"[KB] 加载本地embedding模型: {local_model_path}")
        
        self.embedding_tokenizer = AutoTokenizer.from_pretrained(
            local_model_path, 
            trust_remote_code=True
        )
        self.embedding_model = AutoModel.from_pretrained(
            local_model_path, 
            trust_remote_code=True
        )
        self.embedding_model.eval()
        
        device = "cuda" if torch.cuda.is_available() else "cpu"
        self.embedding_model = self.embedding_model.to(device)
        print(f"[KB] 模型加载完成，使用设备: {device}")
        
        # 批量生成embedding（使用mean pooling）
        # ... 详细代码见完整文件 ...
        
        # 清理模型释放内存
        del self.embedding_model
        del self.embedding_tokenizer
        if device == "cuda":
            torch.cuda.empty_cache()
```

---

## ✅ 修复状态

- ✅ **已修复** `repr()` 方法
- ✅ **已修复** `dict()` 方法
- ✅ **已修改** `_build_embeddings()` 使用本地模型
- ✅ **已修改** 导入部分，不再依赖sentence-transformers

---

## 🚀 现在可以运行

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --tokenizer_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000 \
    --target_count 10 \
    --use-embedding \
    --debug
```

**预期输出**：
```
[INFO] ✓ Embedding依赖已加载（使用本地Qwen3-Embedding模型）
[KB] 🚀 启用语义embedding模式（本地Qwen3-Embedding模型）
[KB] 加载本地embedding模型: /mnt/data/LLM/hhh/qwen3_emb/backup_h/Qwen3-Embedding-0.6B_sft_v5
[KB] 模型加载完成，使用设备: cuda
[KB] 生成 63 个QA的embedding向量...
   进度: 63/63
[KB] ✓ Embedding构建完成（维度: 768）
[KB] ✓ 模型已卸载，内存已释放

[START] 根实体: QA-40
    [提取] 实体 40
    [构建] 基础QA  ← 不会再报错了

[BASE QA] ...
```

---

## 🎉 总结

完整的 `knowledge_base_new.py` 已经生成，包含：

1. ✅ 修复了 `sequence item 0` 错误
2. ✅ 使用本地Qwen3-Embedding模型
3. ✅ 完全离线可用
4. ✅ 兼容新旧两种格式

**直接粘贴到您的文件中即可使用！**
