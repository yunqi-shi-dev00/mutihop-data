# 功能对比总结 📊

## 你的两个问题的答案 ✅

### Q1: 最多几个问题组合？

**答案：默认最多 3 个，可配置（2-4个推荐）**

| 配置 | 参数 | 说明 |
|------|------|------|
| 默认值 | `--max_hops 3` | 最多组合3个问题（推荐） |
| 保守模式 | `--max_hops 2` | 只组合2个问题（更简单） |
| 激进模式 | `--max_hops 4` | 最多4个问题（更复杂，慎用） |

**实现机制**：
```python
# agent_final.py 第532-534行
if num_hops >= self.max_hops:
    print(f"[INFO] 已达到最大跳数限制 ({self.max_hops})，跳过SELECT")
    continue  # 跳过SELECT，不再添加更多问题
```

---

### Q2: 有动态规划组合和消耗策略优化吗？

**答案：有！完整保留并增强！** ✅

---

## 完整功能对比表

| 功能类别 | 原版 | 最终版 | 说明 |
|---------|------|--------|------|
| **核心Action机制** | | | |
| SELECT | ✅ | ✅ | 完全保留，增强了多跳生成 |
| FUZZ | ✅ | ✅ | 完全保留 |
| EXIT | ✅ | ✅ | 完全保留 |
| BRAINSTORM | ✅ | ✅ | 完全保留 |
| **迭代结构** | | | |
| 迭代循环 | ✅ | ✅ | 完全保留 |
| 验证测试 | ✅ | ✅ | 完全保留 |
| **知识库索引** | | | |
| 概念索引 | ✅ | ✅ | 完全保留 |
| 原版find_related_qas | ✅ | ✅ | 完全保留 |
| **🆕 动态规划** | | | |
| 3阶段策略 | ❌ | ✅ | **新增** - 早期/中期/后期 |
| 根QA选择策略 | 随机 | 阶段自适应 | **优化** |
| 邻居QA选择策略 | 概念相似度 | 优先级+概念 | **优化** |
| 论文优先级计算 | ❌ | ✅ | **新增** - 基于使用率 |
| **🆕 消耗统计** | | | |
| 论文使用率追踪 | ❌ | ✅ | **新增** |
| 自动标记完成 | ❌ | ✅ | **新增** - 使用率≥80% |
| 覆盖报告 | ❌ | ✅ | **新增** |
| 低使用率优先 | ❌ | ✅ | **新增** |
| **🆕 多跳优化** | | | |
| 问题组合方式 | 两两组合 | 用所有子QA | **优化** |
| 组合数量限制 | 无限制 | max_hops控制 | **新增** |
| 思维链推理 | ❌ | ✅ | **新增** - `<think>`块 |
| **🆕 质量控制** | | | |
| 问题筛选 | ❌ | ✅ | **新增** - 6大标准 |
| 答案重生成 | ❌ | ✅ | **新增** - 围绕子QA |
| 发散控制 | ❌ | ✅ | **新增** - 强调不发散 |
| **调试功能** | | | |
| 调试输出 | 基础 | 详细 | **增强** - 每阶段标注 |

---

## 动态规划详细机制

### 1. 阶段自适应策略

| 阶段 | 覆盖率范围 | 根QA选择策略 | 邻居QA选择策略 |
|------|-----------|-------------|---------------|
| **早期（Early）** | < 60% | 70%随机 + 30%低使用率 | 优先级排序（概念+使用率） |
| **中期（Mid）** | 60% ~ 80% | 50%随机 + 50%低使用率 | 优先级排序（加大使用率权重） |
| **后期（Late）** | ≥ 80% | 100%低使用率 | 只选择低使用率论文的QA |

**代码位置**：
- 阶段更新：`agent_final.py` 第75-93行
- 根QA选择：`agent_final.py` 第460-476行
- 邻居选择：`agent_final.py` 第546-551行

### 2. 论文优先级计算公式

```python
priority = (1.0 - usage_rate) * 10.0 * quality_score

# 示例：
# 论文A：usage_rate=0.2 → priority=(1-0.2)*10*1.0=8.0 ⭐⭐⭐（高优先级）
# 论文B：usage_rate=0.7 → priority=(1-0.7)*10*1.0=3.0 ⭐（低优先级）
# 论文C：usage_rate=0.9 → priority=(1-0.9)*10*1.0=1.0（几乎不选）
```

**代码位置**：`knowledge_base.py` 第139-151行

### 3. 消耗统计追踪

#### 自动追踪流程
```
每次SELECT成功后 → update_usage()
  ↓
paper_usage[论文] += 1
  ↓
usage_rate = paper_usage / paper_total_qa
  ↓
if usage_rate >= 0.8:
    completed_papers.add(论文)
    active_papers.remove(论文)
    print("[KB] ✓ 论文完成: xxx (使用率: 85.3%)")
```

#### 统计指标
- `paper_usage`: 每篇论文已使用的QA数量
- `paper_total_qa`: 每篇论文的总QA数量
- `paper_usage_rate`: 使用率（0.0 ~ 1.0）
- `completed_papers`: 已完成论文集合（≥80%）
- `active_papers`: 活跃论文集合（<80%）

**代码位置**：`knowledge_base.py` 第24-31行、第171-187行

---

## 工作流程对比

### 原版流程
```
选择根QA（随机）
  ↓
迭代 {
  选择Action
  ↓
  if SELECT:
    找邻居（概念相似度）
    ↓
    组合问题（两两组合）
    ↓
    验证测试
}
```

### 最终版流程
```
选择根QA（动态规划策略）✅
  ↓
迭代 {
  选择Action
  ↓
  if SELECT:
    ⚠️ 检查 num_hops < max_hops ✅
    ↓
    找邻居（优先级+概念）✅
    ↓
    用所有子QA生成多跳问题 ✅
    （思维链推理）
    ↓
    筛选问题（6大标准）✅
    ↓
    重生成答案（围绕子QA）✅
    ↓
    更新消耗统计 ✅
    ↓
    验证测试
}
  ↓
生成覆盖报告 ✅
```

---

## 使用示例

### 完整功能模式（所有优化都启用）
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./generated_qa \
    --model_path /path/to/model \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \      # ✅ 动态规划
    --enable_qa_filtering \          # ✅ 问题筛选
    --enable_answer_regeneration \   # ✅ 答案重生成
    --debug
```

### 原版兼容模式（只保留原功能）
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./original_mode \
    --model_path /path/to/model \
    --batch_size 4 \
    --target_count 50 \
    --max_turns 16 \
    --max_hops 2 \
    --disable_dynamic_planning \     # ❌ 禁用动态规划
    --disable_qa_filtering \         # ❌ 禁用问题筛选
    --disable_answer_regeneration    # ❌ 禁用答案重生成
```

### 动态规划+覆盖优化（无质量控制）
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./coverage_optimized \
    --model_path /path/to/model \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \      # ✅ 动态规划
    --disable_qa_filtering \         # ❌ 禁用筛选（更快）
    --disable_answer_regeneration    # ❌ 禁用重生成（更快）
```

---

## 输出JSON字段

```json
{
  "uid": "生成的唯一ID",
  "question": "最终问题",
  "answer": "最终答案（可能是重生成的）",
  "source_qa_ids": ["qa_001", "qa_002", "qa_003"],
  "source_papers": ["Paper A", "Paper B"],
  "statements": ["陈述1", "陈述2", "陈述3"],
  "edit_history": ["SELECT: ...", "FUZZ: ..."],
  "action_stats": {"SELECT": 2, "FUZZ": 1, "EXIT": 1},
  "num_turns": 5,
  "num_hops": 3,
  "max_hops": 3,                              // ⭐ 新增
  "qa_filtering_enabled": true,               // ⭐ 新增
  "answer_regeneration_enabled": true,        // ⭐ 新增
  "dynamic_planning_enabled": true            // ⭐ 新增
}
```

---

## 性能考虑

### LLM调用次数对比

| 操作 | 原版 | 最终版（全开） | 最终版（只动态规划） |
|------|------|---------------|-------------------|
| choose_action | 1次/轮 | 1次/轮 | 1次/轮 |
| construct_base_qa | 1次 | 1次 | 1次 |
| construct_link_qa | 1次/SELECT | 1次/SELECT | 1次/SELECT |
| **combine_qa** | 1次/SELECT | - | - |
| **generate_multihop** | - | 1次/SELECT | 1次/SELECT |
| **evaluate_question** | - | 1次/SELECT | **0次** |
| **regenerate_answer** | - | 1次/SELECT | **0次** |
| check_info_cover | 1次/SELECT | 1次/SELECT | 1次/SELECT |
| check_qa_valid | 1次 | 1次 | 1次 |
| direct_generate | 4次 | 4次 | 4次 |
| llm_judge_answer | 4次 | 4次 | 4次 |
| **总计（假设2次SELECT）** | ~15次 | ~19次 | ~15次 |

**结论**：
- 完整功能模式：+26%的LLM调用，但质量更高
- 只动态规划模式：调用次数相同，但覆盖更均衡

---

## ✅ 总结

### 你的问题答案

1. **最多组合几个问题**？
   - ✅ 默认3个（`--max_hops 3`）
   - ✅ 可配置2-4个
   - ✅ 代码中有检查保护

2. **有动态规划和消耗策略吗**？
   - ✅ **完整的动态规划策略**（3阶段自适应）
   - ✅ **完整的消耗统计追踪**（论文级别）
   - ✅ **完整的覆盖优化选择**（优先低使用率）
   - ✅ 可通过`--enable_dynamic_planning`开关

### 核心改进

1. **原版核心完全保留** ✅
2. **新增最大跳数限制** ✅
3. **新增动态规划策略** ✅
4. **新增消耗统计追踪** ✅
5. **新增多跳问题生成** ✅
6. **新增质量控制机制** ✅
7. **所有功能可独立开关** ✅

### 推荐配置

**生产环境**（质量优先）：
```bash
--max_hops 3 --enable_dynamic_planning --enable_qa_filtering --enable_answer_regeneration
```

**测试环境**（速度优先）：
```bash
--max_hops 2 --enable_dynamic_planning --disable_qa_filtering --disable_answer_regeneration
```

---

**所有功能完整实现！可以放心使用！** 🎉
