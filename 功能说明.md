# 功能完整说明 ✅

## 1️⃣ 最多组合问题数量限制 ✅

### 功能说明
- **默认值**：最多组合 **3个** 问题（可配置）
- **控制方式**：通过 `--max_hops` 参数设置
- **实现位置**：`agent_final.py` 第532-534行

### 工作原理
```python
# 在SELECT action中检查
if num_hops >= self.max_hops:
    print(f"[INFO] 已达到最大跳数限制 ({self.max_hops})，跳过SELECT")
    continue
```

### 使用示例
```bash
# 默认最多3个问题组合
python main_final.py --input data.jsonl --output ./out

# 自定义最多2个问题组合
python main_final.py --input data.jsonl --output ./out --max_hops 2

# 最多4个问题组合（不推荐，复杂度过高）
python main_final.py --input data.jsonl --output ./out --max_hops 4
```

### 形成过程
```
初始状态: [根QA] → 1跳

第1次SELECT: [根QA, 邻居1] → 2跳
↓ 生成2跳问题+答案

第2次SELECT: [根QA, 邻居1, 邻居2] → 3跳
↓ 生成3跳问题+答案

第3次SELECT: 达到max_hops=3限制 → 跳过 ✅
```

---

## 2️⃣ 动态规划策略 ✅

### 功能说明
- **默认状态**：**启用**
- **控制方式**：`--enable_dynamic_planning` / `--disable_dynamic_planning`
- **实现位置**：`knowledge_base.py` 第101-151行

### 核心机制

#### (1) 生成阶段划分
```python
# agent_final.py 第49-52行
if use_dynamic_planning:
    self.current_stage = 'early'
    self.stage_thresholds = {'early': 0.6, 'mid': 0.2}
```

**阶段定义**：
- **早期阶段（early）**：覆盖率 < 60%
  - 策略：平衡探索，优先低使用率论文
- **中期阶段（mid）**：60% ≤ 覆盖率 < 80%
  - 策略：加强低使用率论文的选择
- **后期阶段（late）**：覆盖率 ≥ 80%
  - 策略：集中补充低覆盖论文

#### (2) 根QA选择策略
```python
# agent_final.py 第460-473行
if self.use_dynamic_planning:
    if self.current_stage == 'early':
        # 早期：70%随机 + 30%低使用率
        if random.random() < 0.7:
            root_qa_id = random.choice(self.kb.qa_ids)
        else:
            root_qa_id = self.kb.select_underutilized_qa()
    
    elif self.current_stage == 'mid':
        # 中期：50%随机 + 50%低使用率
        if random.random() < 0.5:
            root_qa_id = random.choice(self.kb.qa_ids)
        else:
            root_qa_id = self.kb.select_underutilized_qa()
    
    else:  # late
        # 后期：100%低使用率
        root_qa_id = self.kb.select_underutilized_qa()
```

#### (3) 邻居QA选择策略
```python
# agent_final.py 第546-551行
if self.use_dynamic_planning:
    candidates = self.kb.find_related_qas_prioritized(target.id, top_k=10)
else:
    candidates = target.related_qas  # 原版逻辑
```

**优先级计算**：
```python
# knowledge_base.py 第139-151行
def get_paper_priority(self, paper_name: str) -> float:
    """计算论文的选择优先级"""
    if paper_name in self.completed_papers:
        return 0.0  # 已完成论文不再选择
    
    usage_rate = self.paper_usage_rate.get(paper_name, 0.0)
    quality = self.paper_quality_score.get(paper_name, 1.0)
    
    # 使用率越低，优先级越高
    priority = (1.0 - usage_rate) * 10.0
    priority *= quality
    
    return priority
```

---

## 3️⃣ 消耗统计与覆盖策略 ✅

### 功能说明
- **自动追踪**：每次使用QA都更新统计
- **实现位置**：`knowledge_base.py` 第24-31行、第171-187行

### 核心指标

#### (1) 论文级别统计
```python
self.paper_usage = defaultdict(int)        # 每篇论文已使用的QA数量
self.paper_total_qa = defaultdict(int)     # 每篇论文的总QA数量
self.paper_usage_rate = {}                 # 使用率 = 已使用 / 总数量
self.completed_papers = set()              # 已完成论文（使用率≥80%）
self.active_papers = set()                 # 活跃论文（使用率<80%）
```

#### (2) 自动更新机制
```python
# knowledge_base.py 第171-187行
def update_usage(self, qa_ids: List[str]):
    """每次生成后自动调用"""
    for qa_id in qa_ids:
        paper_name = self.qa_to_paper.get(qa_id, 'unknown')
        
        self.paper_usage[paper_name] += 1
        total = self.paper_total_qa[paper_name]
        usage_rate = self.paper_usage[paper_name] / total
        self.paper_usage_rate[paper_name] = usage_rate
        
        # 自动标记完成（使用率≥80%）
        if usage_rate >= 0.8 and paper_name not in self.completed_papers:
            self.completed_papers.add(paper_name)
            self.active_papers.discard(paper_name)
            print(f"[KB] ✓ 论文完成: {paper_name} (使用率: {usage_rate*100:.1f}%)")
```

#### (3) 覆盖统计报告
```python
# knowledge_base.py 第189-217行
def get_usage_stats(self) -> Dict:
    """获取完整的消耗统计"""
    return {
        'total_papers': len(self.paper_to_qas),
        'active_papers': len(self.active_papers),
        'completed_papers': len(self.completed_papers),
        'overall_coverage': completed_papers / total_papers,
        'low_usage_papers': [使用率<20%的论文],
        'high_usage_papers': [使用率≥80%的论文],
        'paper_usage_details': {论文名: 使用率}
    }
```

### 调用时机
```python
# agent_final.py 第560行
self.kb.update_usage([neighbor_id])  # 每次SELECT后更新

# main_final.py（生成报告时）
stats = kb.get_usage_stats()
save_generation_report(args.output, stats, ...)
```

---

## 4️⃣ 完整功能清单

### ✅ 原版功能（完全保留）
- [x] SELECT/FUZZ/EXIT/BRAINSTORM action机制
- [x] 迭代循环结构
- [x] 验证和测试逻辑
- [x] 概念索引和相关QA查找
- [x] 原版find_related_qas方法

### ✅ 新增优化功能
- [x] **多跳问题组合**（每次SELECT用所有子QA）
- [x] **问题数量限制**（max_hops，默认3个）
- [x] **问题筛选**（6大标准评估）
- [x] **答案重生成**（围绕子QA，不发散）
- [x] **动态规划策略**（3阶段自适应）
- [x] **消耗统计追踪**（论文级别使用率）
- [x] **覆盖优化选择**（优先低使用率论文）
- [x] **调试信息输出**（debug模式）

### ✅ 功能开关
```bash
--max_hops 3                        # 最多组合问题数
--max_turns 16                      # 最大迭代轮数
--enable_dynamic_planning           # 启用动态规划（默认）
--disable_dynamic_planning          # 禁用动态规划
--enable_qa_filtering               # 启用问题筛选（默认）
--disable_qa_filtering              # 禁用问题筛选
--enable_answer_regeneration        # 启用答案重生成（默认）
--disable_answer_regeneration       # 禁用答案重生成
--debug                             # 启用调试模式
```

---

## 5️⃣ 完整工作流程

```
【开始】
  ↓
[1] 初始化知识库
  - 构建概念索引 ✅
  - 初始化消耗统计 ✅
  - 设置动态规划阶段 ✅
  ↓
[2] 选择根QA
  - 如果启用动态规划：根据阶段策略选择 ✅
  - 否则：随机选择 ✅
  ↓
[3] 迭代循环（最多max_turns轮）
  ↓
  选择Action（原版机制）✅
  ↓
  if action == 'SELECT':
    ↓
    [3.1] 检查num_hops < max_hops ✅
    ↓
    [3.2] 选择邻居QA
      - 如果启用动态规划：优先级选择 ✅
      - 否则：原版相关QA选择 ✅
    ↓
    [3.3] 构建link_qa + 检查重复 ✅
    ↓
    [3.4] 添加到memory.relevant ✅
    ↓
    [3.5] 用所有子QA生成多跳问题 ✅
      （使用compose_qa_multihop模板）
    ↓
    [3.6] 筛选问题（6大标准）✅
      - 如果未通过：回滚，continue
    ↓
    [3.7] 重生成答案（围绕子QA）✅
      （使用answer_regeneration模板）
    ↓
    [3.8] 更新消耗统计 ✅
    ↓
    num_hops += 1
  ↓
  [4] 验证 + 测试（原版逻辑）✅
  ↓
[5] 保存结果 + 统计报告 ✅
  ↓
【结束】
```

---

## 6️⃣ 配置示例

### 完整功能模式（推荐）
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./generated_qa \
    --model_path /path/to/model \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --debug
```

### 原版模式（仅保留原功能）
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./original_mode \
    --model_path /path/to/model \
    --batch_size 4 \
    --target_count 50 \
    --max_turns 16 \
    --max_hops 2 \
    --disable_dynamic_planning \
    --disable_qa_filtering \
    --disable_answer_regeneration
```

### 快速测试模式
```bash
python main_final.py \
    --input /path/to/QA.jsonl \
    --output ./test \
    --model_path /path/to/model \
    --batch_size 2 \
    --target_count 10 \
    --max_turns 8 \
    --max_hops 2 \
    --debug
```

---

## ✅ 总结

### 回答你的问题

**Q1: 最多几个问题组合？**
- ✅ **默认3个**（通过`--max_hops`配置）
- ✅ 达到限制后自动跳过SELECT
- ✅ 在输出JSON中记录max_hops值

**Q2: 有动态规划组合和消耗策略优化吗？**
- ✅ **有完整的动态规划策略**
  - 3阶段自适应（早期/中期/后期）
  - 根QA选择策略随阶段变化
  - 邻居QA选择基于优先级
- ✅ **有完整的消耗统计追踪**
  - 论文级别使用率统计
  - 自动标记完成论文（≥80%）
  - 优先选择低使用率论文
  - 生成覆盖报告

### 所有功能均已实现并可用！🎉
