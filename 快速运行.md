# 🚀 快速运行指南

## 第一步：安装依赖

```bash
pip install -r requirements.txt
```

---

## 第二步：准备数据

确保你有一个JSONL格式的QA数据文件，每行格式如下：

```json
{"id": "qa_001", "question": "问题1", "answer": "答案1", "paper_name": "Paper A"}
```

---

## 第三步：运行代码

### ⚡ 最简单的运行命令

```bash
python main_final.py \
    --input 你的数据.jsonl \
    --output ./生成结果 \
    --model_path /模型路径 \
    --tokenizer_path /分词器路径
```

### 🔥 推荐的完整命令（带所有优化）

```bash
python main_final.py \
    --input 你的数据.jsonl \
    --output ./generated_qa \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --debug
```

### 🧪 快速测试命令（生成10个）

```bash
python main_final.py \
    --input 你的数据.jsonl \
    --output ./test \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 2 \
    --target_count 10 \
    --max_hops 2 \
    --debug
```

---

## 关键参数说明

| 参数 | 说明 | 默认值 |
|------|------|-------|
| `--input` | **输入数据文件** | 必填 |
| `--output` | **输出目录** | 必填 |
| `--model_path` | **LLM模型路径** | 必填 |
| `--tokenizer_path` | **分词器路径** | 必填 |
| `--max_hops` | **最多组合几个问题** | 3 |
| `--batch_size` | 并发批次大小 | 4 |
| `--target_count` | 目标生成数量 | 50 |
| `--debug` | 显示详细过程 | 关闭 |

---

## 功能开关

### ✅ 启用所有优化（推荐）
```bash
--enable_dynamic_planning \      # 动态规划
--enable_qa_filtering \          # 问题筛选
--enable_answer_regeneration     # 答案重生成
```

### ⚡ 只用动态规划（更快）
```bash
--enable_dynamic_planning \
--disable_qa_filtering \
--disable_answer_regeneration
```

### 📊 原版模式（无优化）
```bash
--disable_dynamic_planning \
--disable_qa_filtering \
--disable_answer_regeneration
```

---

## 查看帮助

```bash
python main_final.py --help
```

---

## 输出结果

运行完成后，在输出目录会看到：

```
./generated_qa/
├── xxx_001.json          # 生成的QA
├── xxx_002.json
├── ...
└── generation_report.json # 统计报告
```

每个JSON文件包含：
- `question`: 最终问题
- `answer`: 最终答案
- `num_hops`: 组合了几个问题
- `source_qa_ids`: 来源QA的ID
- `source_papers`: 来源论文

---

## 🎯 就这么简单！

**最核心的就是这个命令**：

```bash
python main_final.py \
    --input 数据.jsonl \
    --output ./结果 \
    --model_path /模型 \
    --tokenizer_path /分词器
```

其他参数都是可选的，有默认值！

详细参数说明请看：`运行命令.md`
