# 桥联优化方案1：降低桥联阈值 - 修改说明文档

**版本**：v1.0  
**日期**：2025-11-19  
**目标**：提高多跳QA生成成功率（从<10%提升到30-40%）  
**风险等级**：⭐ 低风险（保守优化）

---

## 📊 问题分析

### 当前问题
- **现象**：绝大部分生成的QA都是 `num_hops=1`（单跳QA）
- **原因**：桥联检查过于严格，导致多跳QA生成失败率高
- **数据**：
  ```
  总生成数量: 10
  多跳QA数量: 0-1 (0-10%)  ← 几乎都失败
  ```

### 失败路径
```
[轮1] 构建基础QA → 单跳QA ✓
[轮2] SELECT action → 尝试桥联
    ├─ check_bridge_validity: relevance_score=5, is_valid=no
    └─ 判断：is_valid=no → 拒绝 ❌
[轮3] SELECT action → 再次尝试
    ├─ check_bridge_validity: relevance_score=5, is_valid=no
    └─ 判断：is_valid=no → 拒绝 ❌
[最终] 输出单跳QA (num_hops=1)
```

### 核心问题
**旧逻辑过于依赖LLM的二元判断（yes/no）**，忽略了分数信息：
- LLM判断 `is_valid=no`，但 `relevance_score=5-6` → 也被拒绝
- 实际上分数5-6的桥联质量已经**中等偏上**，应该接受

---

## 🎯 优化目标

| 指标 | 修改前 | 修改后（预期） |
|------|--------|---------------|
| 多跳QA比例 | 0-10% | 30-40% |
| 桥联成功率 | ~20% | ~50% |
| 质量风险 | 无 | 低（分数≥5保证基本质量） |

---

## 🔧 修改方案

### 核心思路
**从"依赖LLM判断"改为"依赖分数阈值"**：
- 旧逻辑：`is_valid=no` → 直接拒绝
- 新逻辑：`relevance_score < 5` → 才拒绝

### 分数含义（0-10分）
```
9-10分：强相关（同一材料/器件的不同方面）
6-8分：中等相关（相关技术领域）✅ 旧阈值
5分：弱相关（表面概念重叠）   ✅ 新阈值
3-4分：弱相关（仅表面重叠）
0-2分：不相关（不同技术领域）
```

**为什么选5分？**
- 5分代表"有一定技术关联，但不是强关联"
- 对于垂直领域（半导体），5分的桥联仍有一定逻辑性
- 保守降低，避免引入明显不相关的QA对

---

## 📂 修改文件

**文件名**：`agent_final_new.py`  
**修改位置**：约第450-480行（SELECT action处理逻辑中）

---

## 📝 详细修改步骤

### 第1步：备份原文件

```bash
cp agent_final_new.py agent_final_new.py.backup
```

### 第2步：找到修改位置

打开 `agent_final_new.py`，搜索关键字：
```python
check_bridge_validity
```

应该能找到这段代码（在SELECT action的处理中）：
```python
# 检查桥联合理性
bridge_check = self.check_bridge_validity(qa1, qa2, statement)
```

### 第3步：定位要替换的代码块

**找到这段代码**（约15-20行）：
```python
# 检查桥联合理性
bridge_check = self.check_bridge_validity(qa1, qa2, statement)
is_valid = bridge_check.get('is_valid', False)
relevance_score = bridge_check.get('relevance_score', 0)

if self.debug:
    print(f"  [SELECT] {'✓' if is_valid else '✗'} 桥联{'合理' if is_valid else '不合理'} (分数: {relevance_score})")
    if not is_valid:
        print(f"  [原因] {bridge_check.get('reason', '')}")

if not is_valid:
    memory_new.relevant.pop()
    memory_new.statements.pop()
    continue
```

### 第4步：替换为新代码

**完整替换为**：
```python
# 检查桥联合理性
bridge_check = self.check_bridge_validity(qa1, qa2, statement)
is_valid = bridge_check.get('is_valid', False)
relevance_score = bridge_check.get('relevance_score', 0)

# ⭐ 新增：分数>=5就接受（即使LLM判断为no）
if not is_valid and relevance_score >= 5:
    is_valid = True
    if self.debug:
        print(f"  [SELECT] ⚠️ 桥联分数{relevance_score}>=5，虽然判断为no但仍接受")

if self.debug:
    print(f"  [SELECT] {'✓' if is_valid else '✗'} 桥联{'合理' if is_valid else '不合理'} (分数: {relevance_score})")
    if not is_valid:
        print(f"  [原因] {bridge_check.get('reason', '')}")

# ⭐ 修改：只看分数，分数<5才拒绝
if relevance_score < 5:
    if self.debug:
        print(f"  [SELECT] ✗ 桥联分数过低 ({relevance_score} < 5)")
    memory_new.relevant.pop()
    memory_new.statements.pop()
    continue
```

### 第5步：保存文件

确保保存后文件编码为UTF-8。

---

## 🔍 修改前后对比

### 核心变化

| 维度 | 修改前 | 修改后 |
|------|--------|--------|
| **判断依据** | `is_valid`（LLM的yes/no） | `relevance_score`（0-10分数） |
| **阈值** | yes才通过（隐含≈6分） | ≥5分就通过 |
| **兼容性** | LLM判no → 直接拒绝 | LLM判no但分≥5 → 接受 |
| **代码行数** | ~10行 | ~15行（+5行新增逻辑） |

### 逻辑流程对比

**修改前的逻辑**：
```
LLM判断 → is_valid = no
          ↓
        直接拒绝 ❌
```

**修改后的逻辑**：
```
LLM判断 → is_valid = no, score = 7
          ↓
        检查分数 → 7 >= 5 → 接受 ✅

LLM判断 → is_valid = no, score = 4
          ↓
        检查分数 → 4 < 5 → 拒绝 ❌

LLM判断 → is_valid = yes, score = 8
          ↓
        双重通过 → 接受 ✅
```

### 新增日志输出

修改后，在debug模式下会看到新的日志：
```
[SELECT] ⚠️ 桥联分数7>=5，虽然判断为no但仍接受
[SELECT] ✓ 桥联合理 (分数: 7)
```

---

## 🧪 测试方法

### 测试命令

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa_test \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --tokenizer_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000 \
    --target_count 10 \
    --use-embedding \
    --debug
```

### 观察要点

#### 1. 日志中的新输出
```
# 修改前看不到这条
[SELECT] ⚠️ 桥联分数5>=5，虽然判断为no但仍接受  ← 新增

# 修改后会看到更多通过的桥联
[SELECT] ✓ 桥联合理 (分数: 5)
[SELECT] ✓ 桥联合理 (分数: 6)
```

#### 2. 最终报告中的统计
```bash
cat ./generated_qa_test/generation_report.json
```

**关注字段**：
```json
{
    "总生成数量": 10,
    "多跳QA数量": 3,  // ← 期望从0-1提升到3-4
    "通过筛选数量": 2  // ← 期望有提升
}
```

#### 3. 检查生成的QA文件
```bash
# 查看多跳QA
grep -l '"num_hops": 2' ./generated_qa_test/*.json
grep -l '"num_hops": 3' ./generated_qa_test/*.json
```

### 对比测试（推荐）

**测试1：修改前（使用备份）**
```bash
mv agent_final_new.py agent_final_new_modified.py
mv agent_final_new.py.backup agent_final_new.py

python main_final_new.py --target_count 10 --output ./qa_before
```

**测试2：修改后**
```bash
mv agent_final_new.py agent_final_new.py.backup
mv agent_final_new_modified.py agent_final_new.py

python main_final_new.py --target_count 10 --output ./qa_after
```

**对比结果**：
```bash
# 修改前
grep '"num_hops"' ./qa_before/*.json | wc -l

# 修改后
grep '"num_hops"' ./qa_after/*.json | wc -l
```

---

## 📊 预期效果

### 数值预期

| 指标 | 修改前 | 修改后（预期） | 提升幅度 |
|------|--------|---------------|---------|
| 多跳QA数量（10个中） | 0-1个 | 3-4个 | +300% |
| 多跳QA比例 | 0-10% | 30-40% | +30pp |
| 桥联接受率 | ~20% | ~50% | +150% |

### 质量预期

**预期生成的多跳QA示例**：
```json
{
    "question": "在IGZO薄膜晶体管中，如何通过控制氧分压调节氧空位浓度，进而优化载流子迁移率？",
    "answer": "...",
    "source_qa_ids": ["12", "34"],
    "num_hops": 2,
    "statements": [
        "氧分压影响IGZO薄膜的氧空位浓度",
        "氧空位浓度直接调控载流子迁移率"
    ]
}
```

### 风险评估

**质量风险**：⭐ 低
- 分数5分仍代表"有一定技术关联"
- 垂直领域（半导体）中，5分桥联通常有基本逻辑性
- 后续仍有 `question_evaluation` 筛选把关

**可能的副作用**：
- 少量（~5%）桥联可能关联性稍弱
- 但通过6大标准筛选后，最终质量有保障

---

## ⚠️ 注意事项

### 1. 务必备份原文件
```bash
cp agent_final_new.py agent_final_new.py.backup
```

### 2. 只修改指定位置
- 不要修改其他代码
- 不要修改缩进
- 确保语法正确

### 3. 测试验证
- 修改后必须测试
- 对比修改前后的效果
- 检查生成的多跳QA质量

### 4. 如果效果不佳
如果修改后多跳率仍 < 20%，可能需要：
- 方案2：进一步降低阈值到4分 + 优化embedding权重
- 方案3：检查 `question_evaluation` 是否过严

---

## 🔄 回滚方法

如果修改后效果不理想，回滚：

```bash
# 恢复原文件
cp agent_final_new.py.backup agent_final_new.py

# 或者手动还原修改
# 将新增的5行代码删除
# 将 if relevance_score < 5: 改回 if not is_valid:
```

---

## 📞 问题排查

### 问题1：修改后仍然全是单跳QA

**可能原因**：
1. 修改位置错误 → 检查是否在SELECT action中
2. 语法错误 → 检查Python缩进
3. 知识库QA关联性太弱 → 考虑方案2

**排查方法**：
```bash
# 检查日志中是否有新的输出
python main_final_new.py --debug | grep "桥联分数"
```

### 问题2：多跳率提升但质量下降

**可能原因**：
- 5分桥联确实有些关联性弱

**解决方法**：
- 调回阈值到5.5或6
- 或者加强 `question_evaluation` 的筛选

### 问题3：程序报错

**可能原因**：
- 语法错误（缩进、引号）
- 修改位置错误

**解决方法**：
```bash
# 检查语法
python -m py_compile agent_final_new.py

# 恢复备份
cp agent_final_new.py.backup agent_final_new.py
```

---

## 📈 后续优化建议

如果方案1效果满意（多跳率30-40%），可以停止。

如果方案1效果不佳（多跳率仍<20%），按顺序尝试：

1. **微调阈值**：从5改为4.5或4
2. **优化embedding权重**（方案2）
3. **放宽筛选标准**（方案3，需谨慎）

---

## 📚 相关文档

- `agent_final_new.py` - Agent主逻辑文件
- `knowledge_base_new.py` - 知识库和embedding逻辑
- `prompts_final.py` - 所有prompt模板
- `check_bridge_validity` prompt - 桥联检查的prompt定义

---

## ✅ 检查清单

修改前：
- [ ] 已备份 `agent_final_new.py`
- [ ] 已阅读本文档
- [ ] 已理解修改原理

修改中：
- [ ] 找到正确的修改位置
- [ ] 完整替换代码块
- [ ] 检查语法和缩进
- [ ] 保存文件

修改后：
- [ ] 运行测试命令
- [ ] 检查debug日志
- [ ] 查看生成报告
- [ ] 对比修改前后效果
- [ ] 抽查多跳QA质量

---

**修改完成后，请运行测试并反馈结果！** 🚀

**如有问题，参考"问题排查"章节或联系技术支持。**
