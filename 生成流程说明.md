# 生成流程详细说明 📖

## 核心概念：是的！在输入QA基础上构建新QA

### 🎯 整体思路

**输入**：你的JSONL文件中的单跳QA（简单问答对）
```json
{"id": "qa_001", "question": "什么是IGZO？", "answer": "IGZO是铟镓锌氧化物...", "paper_name": "Paper A"}
{"id": "qa_002", "question": "IGZO的迁移率是多少？", "answer": "迁移率约为10-50 cm²/Vs...", "paper_name": "Paper B"}
```

**输出**：生成的多跳QA（复杂推理问答）
```json
{
  "question": "IGZO材料如何通过其电子迁移率特性影响薄膜晶体管的开关速度？",
  "answer": "IGZO具有10-50 cm²/Vs的电子迁移率...",
  "num_hops": 3,
  "source_qa_ids": ["qa_001", "qa_002", "qa_003"]
}
```

---

## 📊 完整生成流程（4个阶段）

### 阶段1️⃣：选择根QA（从输入QA中选）

```python
# 代码：agent_final.py 第457-474行
root_id = self.select_root_qa_smart()              # 从输入QA中选一个
root_qa_data = self.kb.get_qa(root_id)            # 获取这个QA的数据
root_entity = SemiconductorQAEntity(root_id, root_qa_data, self.kb)
memory.relevant.append(root_entity)                # 加入memory
```

**说明**：
- 从你的输入JSONL文件中选择一个QA作为"根"
- 如果启用动态规划，会根据使用率智能选择
- 否则随机选择

**示例**：
```
输入QA库有100个 → 选中 qa_001 作为根
```

---

### 阶段2️⃣：构建基础QA（改写根QA）

```python
# 代码：agent_final.py 第476-492行
base_qa = await self.construct_base_qa(root_entity)  # ⭐ 这里！

memory.qa['question'] = base_qa['question']
memory.qa['answer'] = base_qa['answer']
memory.statements.append(base_qa['statement'])
```

**这里就是"基础QA构建"！**

#### 它做了什么？

**输入**：选中的根QA（qa_001）
```
问题：什么是IGZO？
答案：IGZO是铟镓锌氧化物，是一种非晶半导体材料...
```

**通过LLM改写**（使用`base_qa`模板）：
1. 提取核心概念
2. 改写问题（更专业、更通用）
3. 改写答案（更完整、更准确）
4. 生成技术陈述（statement）

**输出**：基础QA
```json
{
  "question": "在薄膜晶体管应用中，IGZO材料的基本特性是什么？",
  "answer": "IGZO（铟镓锌氧化物）是一种非晶半导体材料，具有高电子迁移率、低漏电流、良好的均匀性等特性...",
  "statement": "IGZO是一种高性能非晶半导体材料"
}
```

**关键点**：
- ✅ **是在输入QA基础上改写的**
- ✅ 不是凭空生成，而是基于原始QA的内容
- ✅ 改写的目的：让问题更专业、更通用、更完整

---

### 阶段3️⃣：迭代添加相关QA（多跳构建）

这是核心的**多跳问题生成**阶段！

#### 第1轮SELECT：找到第2个相关QA

```python
# 代码：agent_final.py 第546-551行
# 从输入QA中找到与根QA相关的另一个QA
neighbor_id = random.choice(candidates)             # 比如选中 qa_002
neighbor_entity = SemiconductorQAEntity(neighbor_id, neighbor_data, self.kb)

# 构建桥联关系
link_qa = await self.construct_link_qa(root_entity, neighbor_entity)

# 添加到memory
memory.relevant.append(neighbor_entity)             # 现在有2个子QA
memory.statements.append(link_qa['statement'])
```

**此时**：
```
memory.relevant = [qa_001(IGZO), qa_002(迁移率)]
num_hops = 2
```

#### 用所有子QA生成2跳问题

```python
# 代码：agent_final.py 第599-608行
multihop_result = await self.generate_multihop_question(
    memory.relevant,      # [qa_001, qa_002]
    memory.statements     # 所有陈述
)

q_new = multihop_result['question']       # 2跳问题
reference_answer = multihop_result['answer']  # 参考答案
```

**生成的2跳问题示例**：
```
问题：IGZO材料的电子迁移率特性如何影响其在薄膜晶体管中的应用性能？
答案：IGZO具有10-50 cm²/Vs的高电子迁移率...（基于qa_001和qa_002的内容）
```

#### 第2轮SELECT：找到第3个相关QA

```python
# 再次SELECT，找到qa_003
memory.relevant.append(qa_003)  # 现在有3个子QA

# 用所有3个子QA重新生成3跳问题
multihop_result = await self.generate_multihop_question(
    memory.relevant,      # [qa_001, qa_002, qa_003]
    memory.statements
)
```

**生成的3跳问题示例**：
```
问题：IGZO材料如何通过其电子迁移率特性和界面能级匹配影响薄膜晶体管的开关速度和稳定性？
答案：综合分析...（基于qa_001、qa_002、qa_003的内容）
num_hops = 3  ← 达到max_hops限制，不再SELECT
```

---

### 阶段4️⃣：筛选和优化（可选）

#### 4.1 问题筛选（6大标准）
```python
# 代码：agent_final.py 第614-622行
if self.enable_qa_filtering:
    eval_result = await self.evaluate_question(q_new, memory.relevant)
    
    if not eval_result['passed']:
        # 未通过筛选，回滚，重新选择
        memory.relevant.pop()
        memory.statements.pop()
        continue
```

#### 4.2 答案重生成（围绕子QA）
```python
# 代码：agent_final.py 第626-644行
if self.enable_answer_regeneration:
    regen_result = await self.regenerate_answer(
        q_new,                    # 问题
        reference_answer,         # 参考答案
        memory.relevant,          # 所有子QA
        reasoning_steps           # 推理步骤
    )
    
    final_answer = regen_result['final_answer']  # 最终答案
```

**强调**：答案重生成时严格基于子QA，不发散！

---

## 🔍 关键流程图

```
【输入】你的JSONL文件（100个单跳QA）
   ↓
┌──────────────────────────────────────┐
│ 阶段1：选择根QA                      │
│   从输入中选：qa_001                 │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 阶段2：构建基础QA（改写）            │
│   输入：qa_001 原始内容              │
│   → LLM改写（base_qa模板）           │
│   输出：更专业的QA                   │
│   memory.relevant = [qa_001]         │
│   num_hops = 1                       │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 阶段3：迭代添加相关QA（多跳）        │
│                                      │
│ 【第1次SELECT】                      │
│   从输入中找相关：qa_002             │
│   → 构建桥联（link_qa）              │
│   → memory.relevant = [qa_001, qa_002]│
│   → 用2个子QA生成2跳问题 ⭐          │
│   num_hops = 2                       │
│                                      │
│ 【第2次SELECT】                      │
│   从输入中找相关：qa_003             │
│   → 构建桥联（link_qa）              │
│   → memory.relevant = [qa_001, qa_002, qa_003]│
│   → 用3个子QA生成3跳问题 ⭐          │
│   num_hops = 3 → 达到max_hops限制    │
│                                      │
│ 【不再SELECT】                       │
└──────────────────────────────────────┘
   ↓
┌──────────────────────────────────────┐
│ 阶段4：筛选和优化                    │
│   → 问题筛选（6大标准）              │
│   → 答案重生成（围绕3个子QA）        │
└──────────────────────────────────────┘
   ↓
【输出】生成的多跳QA（保存为JSON）
{
  "question": "3跳问题",
  "answer": "最终答案",
  "num_hops": 3,
  "source_qa_ids": ["qa_001", "qa_002", "qa_003"],
  "source_papers": ["Paper A", "Paper B", "Paper C"]
}
```

---

## 💡 关键理解

### Q1: 基础QA构建是什么？
**A**: 是对从输入QA库中选中的"根QA"进行改写和提炼
- **输入**：原始QA（可能不够专业、不够完整）
- **输出**：改写后的基础QA（更专业、更通用、更完整）
- **目的**：作为多跳问题的起点

### Q2: 是在输入QA上再构建吗？
**A**: 是的！整个流程都是基于输入QA
1. **根QA**：从输入QA中选择
2. **基础QA**：对根QA改写
3. **相关QA**：从输入QA中找相关的
4. **多跳问题**：基于选中的多个输入QA生成

### Q3: 每次生成用几个输入QA？
**A**: 取决于`max_hops`
- `max_hops=2`：使用2个输入QA生成2跳问题
- `max_hops=3`：使用3个输入QA生成3跳问题（默认）
- `max_hops=4`：使用4个输入QA生成4跳问题

### Q4: 输入QA会被修改吗？
**A**: 不会！
- 输入QA只是作为"参考来源"
- 生成的是**全新的QA**
- 但内容严格基于输入QA，不会发散

---

## 📝 完整示例

### 输入：你的QA库（JSONL）

```jsonl
{"id": "qa_001", "question": "什么是IGZO？", "answer": "IGZO是铟镓锌氧化物...", "paper_name": "Paper A"}
{"id": "qa_002", "question": "IGZO的迁移率？", "answer": "10-50 cm²/Vs", "paper_name": "Paper B"}
{"id": "qa_003", "question": "薄膜晶体管的开关原理？", "answer": "通过栅极电压控制...", "paper_name": "Paper C"}
... 还有97个QA
```

### 生成过程（max_hops=3）

```
Step 1: 选择根QA
  → 选中 qa_001

Step 2: 构建基础QA（改写qa_001）
  原始：什么是IGZO？
  改写：在薄膜晶体管应用中，IGZO材料的基本特性是什么？
  memory.relevant = [qa_001]

Step 3: 第1次SELECT
  → 找到相关的 qa_002（迁移率）
  → memory.relevant = [qa_001, qa_002]
  → 生成2跳问题："IGZO材料的迁移率特性如何影响...？"

Step 4: 第2次SELECT
  → 找到相关的 qa_003（开关原理）
  → memory.relevant = [qa_001, qa_002, qa_003]
  → 生成3跳问题："IGZO材料如何通过其迁移率特性影响薄膜晶体管的开关性能？"
  → num_hops=3，达到max_hops限制

Step 5: 筛选+重生成
  → 检查问题是否符合6大标准 ✓
  → 基于3个子QA重新生成答案（不发散）✓
```

### 输出：生成的多跳QA

```json
{
  "uid": "xxx-xxx-xxx",
  "question": "IGZO材料如何通过其电子迁移率特性影响薄膜晶体管的开关性能和稳定性？",
  "answer": "IGZO（铟镓锌氧化物）是一种非晶半导体材料，其10-50 cm²/Vs的高电子迁移率...",
  "num_hops": 3,
  "source_qa_ids": ["qa_001", "qa_002", "qa_003"],
  "source_papers": ["Paper A", "Paper B", "Paper C"],
  "statements": [
    "IGZO是一种高性能非晶半导体材料",
    "IGZO的高迁移率使其适合高速开关应用",
    "薄膜晶体管通过栅极电压控制开关"
  ]
}
```

---

## ✅ 总结

1. **是的，是在输入QA基础上构建！**
2. 基础QA = 改写后的根QA（从输入中选的）
3. 多跳QA = 基于多个输入QA组合生成
4. 输入QA不会被修改，只是作为来源
5. 生成的QA内容严格基于输入QA，不发散

**一句话**：代码会从你的输入QA库中选择2-3个相关的QA，基于它们的内容生成一个新的、更复杂的多跳推理问题！
