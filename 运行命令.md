# 运行命令说明 🚀

## 基本命令格式

```bash
python main_final.py \
    --input <输入文件> \
    --output <输出目录> \
    --model_path <模型路径> \
    --tokenizer_path <分词器路径> \
    [其他参数...]
```

---

## 必需参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--input` | 输入的QA数据文件（JSONL格式） | `./data/QA.jsonl` |
| `--output` | 输出目录 | `./generated_qa` |
| `--model_path` | LLM模型路径（vLLM/SGLang） | `/path/to/model` |
| `--tokenizer_path` | 分词器路径 | `/path/to/tokenizer` |

---

## 重要参数

### 生成控制
| 参数 | 默认值 | 说明 |
|------|-------|------|
| `--batch_size` | 4 | 并发批次大小 |
| `--target_count` | 50 | 目标生成数量 |
| `--max_turns` | 16 | 最大迭代轮数 |
| `--max_hops` | 3 | **最多组合的问题数量** ⭐ |

### 功能开关
| 参数 | 默认 | 说明 |
|------|------|------|
| `--enable_dynamic_planning` | ✅ | 启用动态规划策略 |
| `--disable_dynamic_planning` | ❌ | 禁用动态规划 |
| `--enable_qa_filtering` | ✅ | 启用问题筛选（6大标准） |
| `--disable_qa_filtering` | ❌ | 禁用问题筛选 |
| `--enable_answer_regeneration` | ✅ | 启用答案重生成 |
| `--disable_answer_regeneration` | ❌ | 禁用答案重生成 |
| `--debug` | ❌ | 启用调试模式 |

### LLM配置
| 参数 | 默认值 | 说明 |
|------|-------|------|
| `--api_base` | `http://localhost:8000/v1` | LLM API地址 |
| `--max_concurrent` | 4 | 最大并发请求数 |

---

## 完整示例

### 1️⃣ 快速测试（10个样本）
```bash
python main_final.py \
    --input ./data/QA.jsonl \
    --output ./test_output \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 2 \
    --target_count 10 \
    --max_hops 2 \
    --debug
```

### 2️⃣ 完整功能模式（推荐）
```bash
python main_final.py \
    --input ./data/QA.jsonl \
    --output ./generated_qa \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --debug
```

### 3️⃣ 仅动态规划（无质量控制，更快）
```bash
python main_final.py \
    --input ./data/QA.jsonl \
    --output ./fast_output \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 4 \
    --target_count 100 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \
    --disable_qa_filtering \
    --disable_answer_regeneration
```

### 4️⃣ 原版兼容模式（所有优化禁用）
```bash
python main_final.py \
    --input ./data/QA.jsonl \
    --output ./original_mode \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer \
    --batch_size 4 \
    --target_count 50 \
    --max_turns 16 \
    --max_hops 2 \
    --disable_dynamic_planning \
    --disable_qa_filtering \
    --disable_answer_regeneration
```

---

## 输入数据格式

### JSONL文件格式
每行一个JSON对象：

```json
{"id": "qa_001", "question": "问题1", "answer": "答案1", "paper_name": "Paper A"}
{"id": "qa_002", "question": "问题2", "answer": "答案2", "paper_name": "Paper B"}
{"id": "qa_003", "question": "问题3", "answer": "答案3", "paper_name": "Paper A"}
```

### 必需字段
- `id`: 唯一标识符
- `question`: 问题文本
- `answer`: 答案文本
- `paper_name`: 论文名称（用于消耗统计）

---

## 输出结果

### 输出目录结构
```
./generated_qa/
├── <uid_1>.json          # 生成的QA 1
├── <uid_2>.json          # 生成的QA 2
├── ...
└── generation_report.json # 生成报告
```

### 单个QA文件格式
```json
{
  "uid": "生成的唯一ID",
  "question": "最终问题",
  "answer": "最终答案",
  "source_qa_ids": ["qa_001", "qa_002", "qa_003"],
  "source_papers": ["Paper A", "Paper B"],
  "statements": ["陈述1", "陈述2", "陈述3"],
  "edit_history": ["SELECT: ...", "FUZZ: ..."],
  "action_stats": {"SELECT": 2, "FUZZ": 1, "EXIT": 1},
  "num_turns": 5,
  "num_hops": 3,
  "max_hops": 3,
  "qa_filtering_enabled": true,
  "answer_regeneration_enabled": true,
  "dynamic_planning_enabled": true
}
```

---

## 运行前检查

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 检查LLM服务
确保LLM服务已启动（vLLM或SGLang）：
```bash
# 测试连接
curl http://localhost:8000/v1/models
```

### 3. 准备数据
确保输入文件存在且格式正确：
```bash
# 检查文件
head -5 ./data/QA.jsonl

# 验证JSON格式
cat ./data/QA.jsonl | python -m json.tool > /dev/null && echo "✅ 格式正确"
```

---

## 常见问题

### Q: 如何调整组合的问题数量？
**A**: 使用 `--max_hops` 参数
```bash
--max_hops 2  # 最多2个问题组合
--max_hops 3  # 最多3个问题组合（默认）
--max_hops 4  # 最多4个问题组合
```

### Q: 如何加快生成速度？
**A**: 禁用质量控制功能
```bash
--disable_qa_filtering \
--disable_answer_regeneration
```

### Q: 如何查看详细的生成过程？
**A**: 启用调试模式
```bash
--debug
```

### Q: 如何修改并发数？
**A**: 调整 `--batch_size` 和 `--max_concurrent`
```bash
--batch_size 8 \          # 批次大小
--max_concurrent 8        # 最大并发请求数
```

---

## 最简运行命令

如果你只想快速测试，最简命令：

```bash
python main_final.py \
    --input your_data.jsonl \
    --output ./output \
    --model_path /path/to/model \
    --tokenizer_path /path/to/tokenizer
```

其他参数都会使用默认值。

---

## 查看帮助

```bash
python main_final.py --help
```

这会显示所有可用参数的详细说明。
