# 最新运行命令 🚀

## 📋 问题诊断

您遇到的问题：
```
[WARNING] sentence-transformers未安装，将使用关键词匹配
```

但实际上已经安装：
```
sentence-transformers 2.2.0
```

**可能原因**：
1. 您运行的是 `main_final.py`（旧版本）
2. 旧版本使用的是 `knowledge_base.py`（不支持embedding）
3. 需要使用优化后的文件

---

## ✅ 解决方案

### 方案1：使用新文件（推荐）

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --tokenizer_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000 \
    --batch_size 4 \
    --target_count 1 \
    --max_turns 3 \
    --max_hops 3 \
    --use-embedding \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --enable_bridge_check \
    --debug \
    --merge_output
```

**注意**：
- `main_final.py` → `main_final_new.py`
- 这个文件支持 embedding 和所有优化功能

---

### 方案2：测试embedding依赖

先运行测试脚本，确认环境没问题：

```bash
python test_embedding_import.py
```

**预期输出**：
```
============================================================
测试 sentence-transformers 导入
============================================================

[测试1] 导入 sentence_transformers...
✓ SentenceTransformer 导入成功

[测试2] 导入 numpy...
✓ numpy 导入成功 (版本: 1.24.0)

[测试3] 导入 sklearn...
✓ sklearn.metrics.pairwise.cosine_similarity 导入成功

[测试4] 一次性导入所有依赖...
✓ 所有依赖导入成功！

[测试5] 尝试加载embedding模型...
   正在加载 all-MiniLM-L6-v2 模型（首次会下载）...
✓ 模型加载成功
✓ 编码测试成功 (向量维度: 384)

============================================================
测试完成
============================================================
```

如果测试失败，会显示详细的错误信息。

---

### 方案3：替换旧文件（如果想保持文件名不变）

```bash
# 备份旧文件
mv main_final.py main_final_old.py
mv knowledge_base.py knowledge_base_old.py
mv agent_final.py agent_final_old.py
mv prompts_final.py prompts_final_old.py

# 使用新文件
cp main_final_new.py main_final.py
cp knowledge_base_new.py knowledge_base.py
cp agent_final_new.py agent_final.py
cp prompts_final_new.py prompts_final.py

# 然后继续使用原来的命令
python main_final.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --use-embedding \
    --debug
```

---

## 📊 文件对应关系

| 旧文件（不支持embedding） | 新文件（完全优化版） | 主要改进 |
|---------------------------|---------------------|---------|
| `main_final.py` | `main_final_new.py` | + embedding参数 |
| `knowledge_base.py` | `knowledge_base_new.py` | + embedding支持 |
| `agent_final.py` | `agent_final_new.py` | + JSON容错、桥联检查 |
| `prompts_final.py` | `prompts_final_new.py` | + 强化子QA依赖 |

---

## 🔍 快速检查

### 检查1：您当前使用的是哪个文件？

```bash
# 查看main_final.py的导入
head -30 main_final.py | grep "from knowledge_base"

# 如果看到：
# from knowledge_base import ...  → 旧版本（不支持embedding）
# from knowledge_base_new import ... → 新版本（支持embedding）
```

### 检查2：确认文件存在

```bash
ls -lh *_new.py
```

**应该看到**：
```
-rw-r--r-- 1 root root  45K agent_final_new.py
-rw-r--r-- 1 root root  32K knowledge_base_new.py
-rw-r--r-- 1 root root   8K main_final_new.py
-rw-r--r-- 1 root root  28K prompts_final_new.py
```

---

## 🎯 推荐步骤

### 步骤1：运行测试脚本

```bash
python test_embedding_import.py
```

### 步骤2：如果测试通过，使用新文件运行

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --use-embedding \
    --debug
```

### 步骤3：观察输出

**正确的输出应该是**：
```
[INFO] ✓ sentence-transformers已加载
[KB] 🚀 启用语义embedding模式
[KB] 加载embedding模型（all-MiniLM-L6-v2）...
[KB] 生成 500 个QA的embedding向量...
100%|████████████| 500/500 [00:15<00:00, 32.50it/s]
[KB] ✓ Embedding构建完成（维度: 384）
```

**而不是**：
```
[WARNING] sentence-transformers未安装，将使用关键词匹配
[KB] 使用关键词匹配模式
```

---

## ⚠️ 常见问题

### Q1: 我确实安装了sentence-transformers，为什么还提示未安装？

**A**: 您可能在使用旧版本的代码文件：
- `main_final.py` 导入的是 `knowledge_base.py`（旧版，不支持embedding）
- 应该使用 `main_final_new.py`，它导入的是 `knowledge_base_new.py`（新版，支持embedding）

### Q2: 如何确认是否在使用embedding模式？

**A**: 运行时观察输出：
- ✓ 看到 `[KB] 🚀 启用语义embedding模式` → 正在使用embedding
- ✗ 看到 `[KB] 使用关键词匹配模式` → 未使用embedding

### Q3: 首次运行为什么很慢？

**A**: 首次使用embedding时需要下载模型（约90MB）：
```
[KB] 加载embedding模型（all-MiniLM-L6-v2）...
Downloading: 100%|████████████| 90M/90M [00:30<00:00, 3.0MB/s]
```
下载完成后，后续运行就会很快。

---

## 📝 完整示例

```bash
# 1. 测试环境
python test_embedding_import.py

# 2. 运行生成（使用新文件）
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --tokenizer_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000 \
    --batch_size 4 \
    --target_count 1 \
    --max_turns 3 \
    --max_hops 3 \
    --use-embedding \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --enable_bridge_check \
    --debug \
    --merge_output

# 3. 查看结果
ls -lh generated_qa/
cat generated_qa/*.json | head -50
```

---

## 🚀 快速修复

如果想快速解决，直接运行：

```bash
# 简化命令（只保留必要参数）
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --target_count 1 \
    --use-embedding \
    --debug
```

**关键变化**：
- `main_final.py` → `main_final_new.py` ⭐

就这一个改动即可！
