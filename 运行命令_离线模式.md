# 运行命令 - 离线模式（不使用embedding）🚀

## 🎯 问题诊断

### 问题1：Embedding模型无法下载
```
[KB] ✗ Embedding构建失败: We couldn't connect to 'https://huggingface.co' to load the files
```

**原因**：`all-MiniLM-L6-v2` 模型需要从 HuggingFace 下载，但您的环境无法联网。

**解决方案**：**不使用embedding模式**，使用关键词匹配（效果也不错）。

### 问题2：构建基础QA失败
```
[ERROR] 构建基础QA失败: sequence item 0: expected str instance, dict found
```

**原因**：代码bug，已修复。

---

## ✅ 正确的运行命令（离线模式）

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --tokenizer_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000 \
    --batch_size 4 \
    --target_count 10 \
    --max_turns 16 \
    --max_hops 3 \
    --enable_dynamic_planning \
    --enable_qa_filtering \
    --enable_answer_regeneration \
    --enable_bridge_check \
    --debug \
    --merge_output
```

**关键变化**：
- ✗ 去掉 `--use-embedding` （这个需要联网下载模型）
- ✅ 使用关键词匹配模式（离线可用）

---

## 📊 简化版命令（只保留核心参数）

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --target_count 10 \
    --debug
```

---

## 🔍 两种模式对比

| 模式 | 需要联网 | 相关QA查找准确率 | 首次运行速度 | 推荐场景 |
|------|---------|-----------------|-------------|---------|
| **关键词匹配** | ❌ 不需要 | 70-80% | 快 | **离线环境** |
| **Embedding** | ✅ 需要 | 85-95% | 慢（首次） | 有网络环境 |

**结论**：您的环境无法联网，使用**关键词匹配模式**即可，效果也不错！

---

## 🚀 完整运行示例

### 步骤1：启动vLLM服务器（如果没启动）

```bash
python -m vllm.entrypoints.openai.api_server \
    --model /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --host localhost \
    --port 8000
```

### 步骤2：运行生成（离线模式）

```bash
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --target_count 10 \
    --debug
```

### 步骤3：查看结果

```bash
# 查看生成的文件
ls -lh generated_qa/

# 查看生成报告
cat generated_qa/generation_report.json

# 查看生成的QA
cat generated_qa/merged_qa.jsonl | head -20
```

---

## 📝 预期输出（正确）

```
[KB] 使用关键词匹配模式  ← 这个是正常的，因为没用embedding
[KB] 加载 63 条QA数据
[KB] 论文数量: 6
[KB] 活跃论文: 6

[Agent] ✓ 启用动态规划策略
[Agent] ✓ 启用问题筛选
[Agent] ✓ 启用答案重生成
[Agent] ✓ 启用桥联合理性检查
[Agent] ✓ 启用调试模式

============================================================
[START] 根实体: QA-40
        阶段: early
============================================================

    [提取] 实体 40
    [构建] 基础QA  ← 这里不应该报错了

[BASE QA] 在选择柔性衬底材料时，聚对苯二甲酸乙二醇酯（PET）...

--- 第 1 轮 ---
[ACTION] none - 

[NEW Q] ...

[TEST] 正确率: 2/4
[INFO] 测试通过

[DONE] 已保存: ./generated_qa/xxx.json
       问题: ...
       跳数: 1
```

---

## ⚠️ 如果将来想用embedding（需要联网环境）

### 方案1：预先下载模型

在有网络的机器上：

```bash
# 下载模型
python -c "from sentence_transformers import SentenceTransformer; model = SentenceTransformer('all-MiniLM-L6-v2')"

# 查找模型缓存位置
ls ~/.cache/huggingface/hub/models--sentence-transformers--all-MiniLM-L6-v2/

# 将整个目录拷贝到离线机器
```

### 方案2：使用本地embedding模型

修改 `knowledge_base_new.py` 中的模型路径：

```python
# 第79行左右
self.embedding_model = SentenceTransformer('/path/to/local/model')
```

但现在不需要，**直接用关键词匹配就行**！

---

## 🎯 现在立即运行

```bash
# 复制这个命令直接运行
python main_final_new.py \
    --input /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl \
    --output ./generated_qa \
    --model_path /mnt/data/LLM/lhy/models/Qwen/Qwen2.5-7B-Instruct \
    --target_count 10 \
    --debug
```

**关键点**：
1. ✅ 使用 `main_final_new.py`（不是 `main_final.py`）
2. ✅ **不加** `--use-embedding` 参数
3. ✅ 代码已修复，不会再报错

就这样！直接运行即可！ 🚀
