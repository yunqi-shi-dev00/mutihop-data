# 链接（Link QA）桥联说明 🔗

## 什么是链接（Link QA）？

**链接（Link QA）**是在SELECT阶段，将两个已有的QA实体"桥接"起来的过程。

---

## 🎯 核心作用

链接的作用是：
1. **建立两个QA之间的关联关系**
2. **生成一个"桥接陈述"（statement）**
3. **为后续的多跳问题提供逻辑连接**

---

## 📊 在整个流程中的位置

```
阶段1: 选择根QA (qa_001: IGZO)
   ↓
阶段2: 构建基础QA (改写qa_001)
   memory.relevant = [qa_001]
   ↓
阶段3: 第1次SELECT
   ├─ 找到邻居: qa_002 (迁移率)
   ├─ ⭐ 构建link_qa: 桥接 qa_001 和 qa_002
   │    └─ 输出: statement = "IGZO的高迁移率使其适合高速器件应用"
   ├─ memory.relevant = [qa_001, qa_002]
   ├─ memory.statements = [statement1, statement2]  ⭐
   └─ 用2个QA + 2个statements 生成2跳问题
   ↓
阶段3: 第2次SELECT
   ├─ 找到邻居: qa_003 (开关原理)
   ├─ ⭐ 构建link_qa: 桥接 qa_002 和 qa_003
   │    └─ 输出: statement = "高迁移率影响开关速度"
   ├─ memory.relevant = [qa_001, qa_002, qa_003]
   ├─ memory.statements = [statement1, statement2, statement3]  ⭐
   └─ 用3个QA + 3个statements 生成3跳问题
```

---

## 🔍 详细过程

### 输入：两个QA实体

假设第1次SELECT时：

**实体A（target）**：qa_001（已在memory中）
```json
{
  "id": "qa_001",
  "question": "什么是IGZO？",
  "answer": "IGZO是铟镓锌氧化物，是一种非晶半导体材料...",
  "name": "IGZO"
}
```

**实体B（neighbor）**：qa_002（刚找到的邻居）
```json
{
  "id": "qa_002",
  "question": "IGZO的电子迁移率是多少？",
  "answer": "IGZO的电子迁移率约为10-50 cm²/Vs...",
  "name": "电子迁移率"
}
```

### 处理：构建链接

```python
# 代码：agent_final.py 第577-582行
link_qa = await self.construct_link_qa(target, neighbor_entity)

# 内部调用 prompts_final.py 的 link_qa 模板
# 模板内容：
"""
你是一个半导体领域的QA构建专家。
给定两个半导体概念的信息，构建一个问题，
其中答案是{conceptA}，问题上下文涉及{conceptB}。

概念A (IGZO) 的信息：
  IGZO是铟镓锌氧化物，是一种非晶半导体材料...

概念B (电子迁移率) 的信息：
  IGZO的电子迁移率约为10-50 cm²/Vs...
"""
```

### 输出：链接QA

```json
{
  "question": "在薄膜晶体管应用中，什么材料具有10-50 cm²/Vs的高迁移率特性？",
  "answer": "IGZO（铟镓锌氧化物）",
  "statement": "IGZO材料具有10-50 cm²/Vs的高电子迁移率，这使其非常适合高速薄膜晶体管应用",
  "key_concepts": ["IGZO", "电子迁移率", "薄膜晶体管"]
}
```

**关键**：这个 `statement` 会被添加到 `memory.statements` 中！

---

## 💡 Statement 的作用

### Statement 是什么？

**Statement（技术陈述）**是对两个QA之间关系的简洁描述。

### Statement 用在哪里？

在生成多跳问题时，statement会作为**上下文信息**传递给多跳组合模板：

```python
# 代码：agent_final.py 第599-602行
multihop_result = await self.generate_multihop_question(
    memory.relevant,      # 所有子QA实体
    memory.statements     # 所有statement ⭐
)
```

在多跳模板中：
```python
# prompts_final.py 第55-62行
compose_qa_multihop = '''
...
# 单跳问答列表
{single_hop_qas}

# 技术陈述  ⭐ 这里！
{statements}
...
'''
```

### 示例

**3个QA + 3个statements**：

```
单跳QA-1:
  问题: 什么是IGZO？
  答案: IGZO是铟镓锌氧化物...

单跳QA-2:
  问题: IGZO的迁移率是多少？
  答案: 10-50 cm²/Vs

单跳QA-3:
  问题: 薄膜晶体管的开关原理？
  答案: 通过栅极电压控制...

技术陈述:  ⭐
  1. IGZO是一种高性能非晶半导体材料
  2. IGZO的高迁移率使其适合高速器件应用
  3. 高迁移率直接影响薄膜晶体管的开关速度
```

LLM基于这些信息生成多跳问题：
```
问题: IGZO材料如何通过其电子迁移率特性影响薄膜晶体管的开关性能？
```

---

## 🔗 完整的链接流程

### 第1次SELECT（构建第1个链接）

```
当前memory:
  relevant: [qa_001(IGZO)]
  statements: [基础QA的statement]

SELECT找到: qa_002(迁移率)
  ↓
构建链接:
  link_qa = construct_link_qa(qa_001, qa_002)
  输出: {
    "question": "...",
    "answer": "...",
    "statement": "IGZO的高迁移率使其适合高速应用"  ⭐
  }
  ↓
添加到memory:
  memory.relevant.append(qa_002)
  memory.statements.append("IGZO的高迁移率使其适合高速应用")
  ↓
现在:
  relevant: [qa_001, qa_002]
  statements: [statement1, statement2]
  ↓
生成2跳问题（基于2个QA + 2个statements）
```

### 第2次SELECT（构建第2个链接）

```
当前memory:
  relevant: [qa_001, qa_002]
  statements: [statement1, statement2]

SELECT找到: qa_003(开关原理)
  ↓
构建链接:
  link_qa = construct_link_qa(qa_002, qa_003)  ⭐ 注意：是qa_002和qa_003
  输出: {
    "statement": "高迁移率直接影响开关速度"  ⭐
  }
  ↓
添加到memory:
  memory.relevant.append(qa_003)
  memory.statements.append("高迁移率直接影响开关速度")
  ↓
现在:
  relevant: [qa_001, qa_002, qa_003]
  statements: [statement1, statement2, statement3]
  ↓
生成3跳问题（基于3个QA + 3个statements）
```

---

## 🎯 关键理解

### Q1: Link QA 和多跳问题的区别？

| 项目 | Link QA | 多跳问题 |
|------|---------|----------|
| **作用** | 桥接两个QA | 组合所有QA生成最终问题 |
| **输入** | 2个QA实体 | 所有已收集的QA实体 |
| **输出** | statement（技术陈述） | 完整的多跳问题+答案 |
| **使用** | 提供给多跳模板的上下文 | 最终输出 |

### Q2: 为什么需要 Link QA？

1. **建立逻辑连接**：明确两个QA之间的关系
2. **提供上下文**：statement帮助LLM理解QA之间的关联
3. **避免孤立**：确保多跳问题不是简单拼接，而是有逻辑链条

### Q3: Link QA 会被保存吗？

**不会！** Link QA只是中间产物：
- ✅ **statement 会被保存**到 `memory.statements`
- ✅ statement 会传递给多跳模板
- ❌ link_qa 的 question 和 answer **不会被使用**
- ❌ 最终输出中**不包含** link_qa 本身

### Q4: 每次SELECT都会构建 Link QA 吗？

**是的！** 每次SELECT都会：
1. 找到一个新的邻居QA
2. 构建 link_qa（桥接当前target和新邻居）
3. 提取 statement
4. 用所有QA + 所有statements 生成新的多跳问题

---

## 📝 代码位置

### 构建 Link QA
```python
# agent_final.py 第186-200行
async def construct_link_qa(self, entityA, entityB):
    prompt = SemiconductorQAPrompts.link_qa.format(
        conceptA=entityA.name,
        conceptB=entityB.name,
        contentA=entityA.repr(),
        contentB=entityB.repr()
    )
    text = await self.call_llm(prompt)
    link_qa = json.loads(text)
    return link_qa
```

### 使用 Link QA
```python
# agent_final.py 第577-590行
# (3) 构建link_qa
link_qa = await self.construct_link_qa(target, neighbor_entity)

# (4) 检查重复（检查statement是否重复）
duplicate = await self.check_info_cover(
    link_qa['statement'],
    memory_new.statements_repr()
)

if duplicate:
    print("[WARNING] 陈述重复")
    continue

# (5) 添加到memory
memory_new.relevant.append(neighbor_entity)
memory_new.statements.append(link_qa['statement'])  ⭐
```

---

## 🎨 可视化流程

```
┌─────────────────────────────────────────────┐
│  第1次SELECT                                │
├─────────────────────────────────────────────┤
│  输入:                                      │
│    target: qa_001 (IGZO)                   │
│    neighbor: qa_002 (迁移率)               │
│                                             │
│  ↓ construct_link_qa                       │
│                                             │
│  LLM生成:                                   │
│    question: "什么材料有高迁移率？"        │
│    answer: "IGZO"                          │
│    statement: "IGZO的高迁移率使其适合..." │
│                                             │
│  ↓ 提取statement                           │
│                                             │
│  添加到memory:                              │
│    memory.statements.append(statement)     │
│                                             │
│  ↓ 用于多跳生成                            │
│                                             │
│  传递给多跳模板:                            │
│    单跳QA: [qa_001, qa_002]                │
│    技术陈述: [statement1, statement2] ⭐   │
│                                             │
│  ↓ 生成2跳问题                             │
└─────────────────────────────────────────────┘
```

---

## ✅ 总结

1. **Link QA 是桥接工具**，不是最终输出
2. **每次SELECT都会构建一个 link_qa**
3. **Statement 是关键**，会传递给多跳模板
4. **作用是建立逻辑连接**，让多跳问题更连贯
5. **最终输出的多跳问题**是基于所有QA + 所有statements生成的

**一句话**：Link QA 像"胶水"，把多个输入QA粘合起来，statement 是"标签"，标注了它们之间的关系！
