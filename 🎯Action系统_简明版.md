# 🎯 Action系统 - 简明版

**用户问题**：action干啥？它不管理跳数吗？EXIT啥作用？

---

## 📚 **三个独立的机制**

```
┌─────────────────────────────────────┐
│  1️⃣ 跳数控制（策略层）              │
│  - 设定目标：target_hops = 1-4随机  │
│  - 检查限制：num_hops >= target?    │
│  - 决定：能不能执行SELECT           │
└─────────────────────────────────────┘
              ↓ 约束
┌─────────────────────────────────────┐
│  2️⃣ Action系统（操作层）            │
│  - LLM选择：做什么                  │
│  - SELECT：组合QA（如果允许）       │
│  - FUZZ：模糊化问题                 │
│  - EXIT：主动退出（如果允许）       │
└─────────────────────────────────────┘
              ↓ 评估
┌─────────────────────────────────────┐
│  3️⃣ EXIT机制（质量层）              │
│  - 判断：问题够不够好？             │
│  - 触发：ready_to_exit = True       │
│  - 效果：退出循环，保存结果         │
└─────────────────────────────────────┘
```

---

## 1️⃣ **跳数控制**

### 作用

**系统层面的"最多几跳"限制**

---

### 机制

```python
# 每个QA开始时
target_hops = random.randint(1, 4)  # 1-4随机

# SELECT执行前
if num_hops >= target_hops:
    continue  # 达到目标，跳过SELECT

# SELECT成功后
num_hops += 1  # 跳数+1
```

---

### 效果

| 当前跳数 | 目标跳数 | 能否SELECT |
|---------|---------|-----------|
| 1 | 2 | ✅ 可以 |
| 2 | 2 | ❌ 达到目标 |
| 2 | 4 | ✅ 可以 |
| 4 | 4 | ❌ 达到目标 |

---

## 2️⃣ **Action系统**

### 作用

**LLM每轮选择的"操作指令"**

---

### 4种Action

| Action | 作用 | 影响跳数 | 何时可选 |
|--------|------|----------|----------|
| `none` | 构建基础QA | ❌ | 仅第1轮 |
| `SELECT` | 组合相关QA | ✅ +1 | 任意轮 |
| `FUZZ` | 模糊化问题 | ❌ | 任意轮 |
| `EXIT` | 主动退出 | ❌ | ready=True时 |

---

### SELECT详解

```python
if action['action'] == 'SELECT':
    # ⚠️ 先检查跳数
    if num_hops >= target_hops:
        print("已达到目标跳数，跳过")
        continue  # 不执行SELECT
    
    # ✅ 执行SELECT
    # 1. 选择邻居QA
    # 2. 桥联检查
    # 3. 多跳组合
    # 4. 筛选评估
    
    # ✅ 成功后
    num_hops += 1
    ready_to_exit = True
```

**关键**：
- LLM可以选择SELECT
- 但系统会检查跳数限制
- 达到目标就跳过（不执行）

---

### FUZZ详解

```python
if action['action'] == 'FUZZ':
    # 模糊化问题（不影响跳数）
    q_new = action['question']
    # num_hops不变
```

**用途**：
- 增加问题难度
- 不增加跳数
- 优化问题表述

---

## 3️⃣ **EXIT机制**

### 作用

**LLM的"质量满意信号"**（不是跳数管理）

---

### 触发条件

#### 条件1：ready_to_exit = True

```python
if ready_to_exit:
    actions.append(SemiconductorQAPrompts.EXIT)
```

**什么时候ready_to_exit = True？**
- SELECT成功后
- 筛选通过后

**意义**：至少组合了1次，有质量保障

---

#### 条件2：LLM自主判断

**LLM会评估**：
- 问题是否足够复杂？
- 答案是否完整？
- 推理链是否清晰？

**如果满意，选择EXIT**：
```json
{
    "action": "EXIT",
    "note": "问题已足够复杂，涵盖多个机制"
}
```

---

### 效果

```python
if action['action'] == 'EXIT':
    print("[INFO] 退出")
    break  # 退出循环，保存结果
```

---

### ❌ 不是强制的

即使可以EXIT，LLM也可能选择：
- 继续SELECT（如果未达跳数）
- 继续FUZZ（优化问题）

---

## 📊 **完整示例**

### 目标跳数：2

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第 1 轮】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LLM选择：Action = none
执行：构建基础QA
结果：
  - num_hops = 1
  - ready_to_exit = False

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第 2 轮】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LLM选择：Action = SELECT

检查：
  num_hops (1) >= target_hops (2)? → ❌ 未达到
  → ✅ 可以执行SELECT

执行：
  1. 选择邻居：6460
  2. 桥联检查：✓
  3. 多跳组合：✓
  4. 筛选评估：✓

结果：
  - num_hops = 2 ✅
  - ready_to_exit = True
  - EXIT现在可选了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第 3 轮】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LLM选择：Action = SELECT （LLM还想组合）

检查：
  num_hops (2) >= target_hops (2)? → ✅ 达到目标
  → ❌ 跳过SELECT

结果：
  - num_hops = 2 (不变)
  - continue (下一轮)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第 4 轮】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LLM选择：Action = FUZZ

执行：
  模糊化问题...

结果：
  - num_hops = 2 (不变)
  - 问题变得更有挑战性

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第 5 轮】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LLM选择：Action = EXIT

LLM思考：
  - 问题已经很复杂 ✓
  - 包含2个QA组合 ✓
  - 推理链清晰 ✓
  - 可以退出了

执行：
  break（退出循环）

结果：
  - 保存QA
  - num_hops = 2 / target_hops = 2 ✅
  - final_qa_count = 2个 ✅
```

---

## 🎯 **核心总结**

### Action不直接管理跳数

| 机制 | 职责 |
|------|------|
| **跳数控制** | 决定"能不能"执行SELECT |
| **Action系统** | 决定"做不做"SELECT/FUZZ/EXIT |
| **EXIT机制** | 决定"好不好"，是否退出 |

---

### Action的作用

```
Action = LLM的"操作菜单"

每轮LLM选择：
  - SELECT：我想组合QA（增加深度）
  - FUZZ：我想模糊化问题（增加难度）
  - EXIT：我觉得够好了（质量满意）

系统响应：
  - SELECT：先检查跳数，允许就执行
  - FUZZ：直接执行（不影响跳数）
  - EXIT：直接退出（保存结果）
```

---

### EXIT的作用

```
EXIT ≠ 跳数管理
EXIT = 质量评估 + 主动退出

触发：
  1. ready_to_exit = True（至少1次组合）
  2. LLM认为质量满意

效果：
  - 退出循环
  - 保存当前QA
  - 不等达到max_turns
```

---

## 💡 **一句话总结**

```
跳数控制：系统说"最多几跳"（target_hops）
Action系统：LLM说"我要做什么"（SELECT/FUZZ/EXIT）
EXIT机制：LLM说"够好了，可以了"（质量满意）
```

---

## 🔄 **协同工作**

```
1. 系统：target_hops = 3（随机）
   ↓
2. LLM：我选SELECT
   ↓
3. 系统：检查跳数...允许
   ↓
4. 执行：组合成功，num_hops = 2
   ↓
5. LLM：我再选SELECT
   ↓
6. 系统：检查跳数...允许
   ↓
7. 执行：组合成功，num_hops = 3
   ↓
8. LLM：我再选SELECT
   ↓
9. 系统：检查跳数...达到目标，跳过
   ↓
10. LLM：那我选EXIT
    ↓
11. 系统：退出，保存结果 ✅
```

---

**理解了吗？** 🎓

**关键**：
1. **Action**是LLM的"操作选择"
2. **跳数控制**是系统的"限制检查"
3. **EXIT**是LLM的"质量判断"

**三者独立又协同！** ✨
