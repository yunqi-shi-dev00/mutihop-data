# 🎯 多跳组合 - 简明版

**用户问题**：
1. statements啥作用？
2. 建立桥联的逻辑是啥？
3. 怎么组合的？

---

## 1️⃣ **Statements（技术陈述）**

### 简单说

**Statement = 一句话概括一个QA的核心技术关系**

---

### 例子

**QA-1647**：
- **问题**：PFM与KPFM如何在铁电材料表征中互补？
- **答案**：PFM检测压电响应，KPFM测量表面功函数
- **Statement（简洁概括）**：
  ```
  PFM与KPFM通过机械形变和电势测量互补揭示极化方向和功函数
  ```

---

### 作用

| 作用 | 说明 |
|------|------|
| ✅ **生成多跳问题** | 给LLM提供简洁的技术陈述作为上下文 |
| ✅ **避免重复** | 检查新QA的statement是否与已有的重复 |
| ✅ **展示关系** | 在memory中展示所有QA的技术联系 |

---

### 数据结构

```python
memory.relevant = [QA-1647, QA-6460]  # 2个源QA

memory.statements = [
    "PFM与KPFM通过机械形变和电势测量...",  # QA-1647的statement
    "有机硅通过氢的钝化抑制界面缺陷..."    # QA-6460的statement
]

# 数量相同
len(memory.relevant) == len(memory.statements)  # True
```

---

### 如何使用？

#### 用途1：生成多跳问题

```python
# 生成多跳问题时
multihop_result = await self.generate_multihop_question(
    all_sub_qas=[QA-1647, QA-6460],  # 所有子QA
    statements=[s1, s2]  # ⭐ 传递statements
)

# LLM看到：
"""
【子QA 1】
问题: ...
答案: ...

【子QA 2】
问题: ...
答案: ...

【技术陈述】
- PFM与KPFM通过机械形变和电势测量...
- 有机硅通过氢的钝化抑制界面缺陷...

请生成一个2跳问题，综合上述技术陈述...
"""
```

---

#### 用途2：检查重复

```python
# SELECT时检查新QA是否重复
duplicate = await self.check_info_cover(
    new_statement,  # 新QA的statement
    memory.statements_repr()  # 已有的所有statements
)

if duplicate:
    print("信息重复，跳过")
```

---

## 2️⃣ **桥联（Bridge）检查**

### 简单说

**桥联检查 = 验证两个QA能否合理组合**

```
QA1 ← 已组合
  ↓
 桥联？（检查联系）
  ↓
QA2 ← 候选
```

---

### 检查流程

```
1. 生成桥联陈述（link_qa）
   ├─ 描述两个QA的技术联系
   └─ 例："有机硅的氢钝化与载流子调控共同影响器件性能"

2. LLM评估桥联有效性
   ├─ 概念重叠？
   ├─ 逻辑联系？
   └─ 推理深度？
   
3. 打分（0-10）
   ├─ 8-10分：高相关性 ✅
   ├─ 5-7分：中等相关性 ✅（放宽阈值后接受）
   ├─ 2-4分：低相关性 ✅（放宽阈值后仍接受）
   └─ 0-1分：无相关性 ❌（拒绝）
   
4. 判断
   if score >= 2:  # ⭐ 放宽阈值（原来是6）
       接受 ✅
   else:
       拒绝 ❌
```

---

### 例子

#### 例子1：高相关性（分数8）

**QA1**：PFM与KPFM在铁电材料表征  
**QA2**：有机硅与SiO₂对TFT器件的影响  
**桥联陈述**：都涉及界面工程和材料特性

**LLM评估**：
```json
{
  "relevance_score": 8,
  "reason": "两个QA都涉及界面特性，有明确技术关联"
}
```

**结果**：✅ 通过（8 >= 2）

---

#### 例子2：低相关性（分数1）

**QA1**：太阳能电池的光电转换  
**QA2**：DRAM存储器的刷新机制  
**桥联陈述**：都属于半导体器件

**LLM评估**：
```json
{
  "relevance_score": 1,
  "reason": "完全不同的应用领域，无明确联系"
}
```

**结果**：❌ 拒绝（1 < 2）

---

### 为什么放宽阈值？

| 阈值 | 通过率 | 问题 |
|------|--------|------|
| 6 | 20% | 太严格 ❌ |
| 2 | 70% | 合理 ✅ |

**原因**：
- 阈值6时，很多合理的组合被拒绝
- 降到2后，保留更多多跳组合
- 筛选还会进一步保证质量

---

## 3️⃣ **组合流程**

### 完整流程图

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第1轮】初始化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Action: none

步骤：
1. 随机选择根QA（如：1647）
2. 提取信息（关键概念、摘要）
3. 构建基础QA

结果：
  memory.relevant = [QA-1647]
  memory.statements = [s1]
  num_hops = 1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第2轮】组合
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Action: SELECT

步骤：
1. 检查跳数
   num_hops (1) < target_hops (2)? → ✅ 可以

2. 选择邻居
   candidates = [6460, 7392, ...]
   选择: 6460

3. 【桥联检查】
   ├─ 生成link_qa（桥联陈述）
   ├─ check_bridge_validity
   │   └─ relevance_score = 6 >= 2 ✅
   └─ 通过

4. 【信息去重】
   └─ 不重复 ✅

5. 【多跳组合】
   ├─ memory_new.relevant = [1647, 6460]
   ├─ memory_new.statements = [s1, s2]
   └─ generate_multihop_question(...)
       → 生成2跳问题

6. 【筛选评估】
   └─ 6个标准 → 通过 ✅

7. 【答案重生成】
   └─ 基于2个子QA重生成答案

8. 【直接接受】
   memory = memory_new
   num_hops = 2

结果：
  memory.relevant = [QA-1647, QA-6460]
  memory.statements = [s1, s2]
  num_hops = 2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第3轮】达到目标
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Action: SELECT

步骤：
1. 检查跳数
   num_hops (2) >= target_hops (2)? → ❌ 达到目标
   
2. 跳过SELECT

结果：memory不变

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【第4轮】退出
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Action: EXIT

步骤：
1. LLM认为质量满意
2. 退出循环

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【保存】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{
  "question": "2跳问题（组合了2个QA）",
  "answer": "基于2个子QA的综合答案",
  "source_qa_ids": [1647, 6460],
  "statements": [s1, s2],
  "num_hops": 2,
  "final_qa_count": 2
}
```

---

### 核心步骤详解

#### 步骤1：选择邻居

```python
# 从已组合的QA中选一个
target = random.choice(memory.relevant)

# 找它的相关QA（top 30）
candidates = kb.find_related_qas(target.id, top_k=30)

# 随机选前5个之一
neighbor_id = random.choice(candidates[:5])
```

---

#### 步骤2：桥联检查

```python
# 生成桥联陈述
link_qa = await build_link_qa(target, neighbor)
# 例：{"statement": "有机硅通过氢钝化...", "reason": "..."}

# 检查桥联有效性
bridge = await check_bridge_validity(target, neighbor, link_qa['statement'])
# 例：{"relevance_score": 6, "is_valid": True, "reason": "..."}

# 判断
if bridge['relevance_score'] < 2:
    continue  # 拒绝
else:
    print("✓ 桥联合理")  # 接受
```

---

#### 步骤3：多跳组合

```python
# 添加新QA
memory_new.relevant.append(neighbor)
memory_new.statements.append(link_qa['statement'])

# ⭐ 生成多跳问题
multihop_result = await generate_multihop_question(
    all_sub_qas=memory_new.relevant,  # [1647, 6460]
    statements=memory_new.statements  # [s1, s2]
)

# 结果
q_new = multihop_result['question']  # 2跳问题
reference_answer = multihop_result['answer']  # 参考答案
```

**`generate_multihop_question`内部**：

```python
# 格式化子QA
single_hop_str = """
【子QA 1】
问题: PFM与KPFM如何互补？
答案: PFM检测压电响应，KPFM测量功函数

【子QA 2】
问题: 有机硅与SiO₂如何影响TFT？
答案: 有机硅钝化缺陷，SiO₂提供介质层
"""

# 格式化statements
statements_str = """
- PFM与KPFM通过机械形变和电势测量...
- 有机硅通过氢钝化抑制界面缺陷...
"""

# ⭐ 调用LLM
prompt = f"""
你有{len(all_sub_qas)}个子QA：
{single_hop_str}

技术陈述：
{statements_str}

请生成一个{len(all_sub_qas)}跳问题，综合上述技术陈述，
并提供答案和推理步骤。
"""

# LLM生成
result = await llm(prompt)
# 返回：{"question": "...", "answer": "...", "reasoning_steps": [...]}
```

---

#### 步骤4：筛选评估

```python
# 6个标准评估
eval_result = await evaluate_question(memory_new.repr())
# 标准：因果性、完整性、可追溯性、普遍性、完备性、单一性

if not eval_result['passed']:
    print(f"✗ 未通过筛选：{eval_result['reason']}")
    memory_new.relevant.pop()  # 回滚
    memory_new.statements.pop()
    continue

print("✓ 通过筛选")
```

---

#### 步骤5：答案重生成

```python
# 基于子QA重生成答案
regen_result = await regenerate_answer(
    q_new,  # 多跳问题
    reference_answer,  # 参考答案
    memory_new.relevant,  # 所有子QA
    reasoning_steps  # 推理步骤
)

final_answer = regen_result['final_answer']
memory_new.qa['answer'] = final_answer
```

---

#### 步骤6：直接接受

```python
# ⭐ 优化7：移除测试，直接接受
memory = memory_new
ready_to_exit = True
num_hops += 1

print("[INFO] ✓ 筛选通过，直接接受（已移除测试环节）")
```

---

## 🎯 **总结**

### 1. Statements

```
作用：
  ✅ 生成多跳问题的简洁上下文
  ✅ 检查信息是否重复
  ✅ 展示QA之间的技术联系

数据：
  memory.relevant = [QA1, QA2]
  memory.statements = [s1, s2]
  长度相同
```

---

### 2. 桥联检查

```
流程：
  生成桥联陈述
    ↓
  LLM评估相关性（0-10分）
    ↓
  if score >= 2:  # 放宽阈值
      接受 ✅
  else:
      拒绝 ❌

目的：
  确保组合的QA有逻辑联系
```

---

### 3. 组合流程

```
选择邻居
  ↓
桥联检查（>= 2）
  ↓
信息去重
  ↓
多跳组合
  ↓
筛选评估（6个标准）
  ↓
答案重生成
  ↓
直接接受（移除测试）
  ↓
更新memory
```

---

### 关键点

| 概念 | 说明 |
|------|------|
| **Statements** | 简洁的技术陈述（一句话概括） |
| **桥联检查** | 评估两个QA的联系（分数 >= 2） |
| **多跳组合** | 基于所有子QA + statements生成新问题 |
| **放宽阈值** | 桥联从6→2，测试移除，提升成功率 |

---

**理解了吗？** 🎓

**3句话总结**：
1. **Statements**：每个QA的一句话技术概括
2. **桥联检查**：确保两个QA有联系（分数 >= 2）
3. **组合流程**：选邻居 → 桥联 → 去重 → 组合 → 筛选 → 接受
