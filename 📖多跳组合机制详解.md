# 📖 多跳组合机制详解

**用户问题**：
1. statements啥作用？
2. 建立桥联的逻辑是啥？
3. 怎么组合的？

---

## 1️⃣ **Statements（技术陈述）的作用**

### 定义

**Statements = 技术陈述列表**

每个statement是一个**简洁的技术总结**，概括了一个QA对的核心技术关系。

---

### 数据结构

**在AgentMemory中**：

```python
class AgentMemory:
    def __init__(self):
        self.qa = dict(question=None, answer=None)  # 当前问题和答案
        self.statements = []  # ⭐ 技术陈述列表
        self.relevant = []    # 已组合的源QA实体列表
        self.edit_history = []
```

**关系**：
- `statements[i]` 对应 `relevant[i]` 的技术陈述
- 数量相同：`len(statements) == len(relevant)`

---

### 例子

#### 单个QA的Statement

**QA-1647**：
- **问题**：对比PFM与KPFM在铁电材料表征中的互补性
- **答案**：PFM通过机械形变检测压电响应，KPFM通过电势测量表面功函数
- **Statement**：
  ```
  PFM与KPFM在铁电材料表征中分别通过机械形变和电势测量，
  互补揭示极化方向和功函数，且通过物理检测原理差异
  （压电效应 vs 电势效应）实现信息互补。
  ```

**作用**：用简洁的语言概括核心技术关系

---

#### 多个Statements的组合

**当组合3个QA时**：

```python
memory.relevant = [QA-1647, QA-6460, QA-7392]  # 3个源QA

memory.statements = [
    "PFM与KPFM在铁电材料表征中...",      # QA-1647的陈述
    "有机硅通过氢的钝化抑制SiO₂界面缺陷...",  # QA-6460的陈述
    "高温沉积工艺对载流子浓度调控..."      # QA-7392的陈述
]
```

---

### 作用1：生成多跳问题的上下文

**在`generate_multihop_question`中**：

```python
async def generate_multihop_question(self, all_sub_qas: List[SemiconductorQAEntity],
                                    statements: List[str]) -> Optional[Dict]:
    """基于所有子QA和statements生成多跳问题"""
    
    # 格式化所有子QA
    single_hop_str = '\n\n'.join([
        f"【子QA {i+1}】\n问题: {qa.qa_data['question']}\n答案: {qa.qa_data['answer']}"
        for i, qa in enumerate(all_sub_qas)
    ])
    
    # ⭐ 格式化技术陈述
    statements_str = "\n".join(statements)
    
    # ⭐ 传递给LLM生成多跳问题
    prompt = SemiconductorQAPrompts.compose_qa_multihop.format(
        num_hops=num_hops,
        single_hop_qas=single_hop_str,
        statements=statements_str  # ← 提供技术陈述作为上下文
    )
```

**为什么需要statements？**

1. **简洁概括**：statements比完整QA更简洁，LLM更容易理解
2. **技术关系**：突出QA之间的技术联系
3. **生成指导**：帮助LLM生成更连贯的多跳问题

---

### 作用2：检查信息覆盖（避免重复）

**在SELECT时检查新QA是否重复**：

```python
# 检查新statement是否与已有statements重复
duplicate = await self.check_info_cover(
    link_qa['statement'],  # 新QA的statement
    memory_new.statements_repr()  # 已有的所有statements
)

if duplicate:
    print("信息重复，跳过")
    continue
```

**为什么需要？**
- 避免组合重复的QA
- 确保每个QA都带来新信息

---

### 作用3：展示给用户/LLM

**在`memory.repr()`中**：

```python
def repr(self):
    statements = '\n'.join(self.statements)
    return f"""
当前问题: {self.qa['question']}
当前答案: {self.qa['answer']}

相关技术陈述：
```txt
{statements}  # ← 展示所有技术陈述
```

相关QA实体列表（已组合的源QA）：
...
"""
```

**用途**：
- LLM在`choose_action`时看到所有技术陈述
- 帮助LLM决定下一步操作

---

## 2️⃣ **桥联（Bridge）的逻辑**

### 什么是桥联？

**桥联 = 检查两个QA之间的连接是否合理**

```
QA1（已组合）
    ↓
   桥联？（检查）
    ↓
QA2（候选）
```

**目的**：确保组合的QA之间有**逻辑联系**，而不是随机拼接

---

### 桥联检查流程

#### 步骤1：生成link_qa（桥联陈述）

**代码**：`build_link_qa(target, neighbor_entity)`

```python
link_qa = await self.build_link_qa(target, neighbor_entity)
```

**link_qa包含**：
- `statement`：桥联技术陈述（描述两个QA的联系）
- `reason`：为什么这两个QA可以组合

**例子**：

```python
link_qa = {
    'statement': "有机硅通过氢的钝化抑制SiO₂界面缺陷，而有机硅层中氢的扩散提供载流子浓度，二者共同提升TFT器件的迁移率。",
    'reason': "两个QA都涉及界面工程和载流子调控"
}
```

---

#### 步骤2：检查桥联有效性

**代码**：`check_bridge_validity(qa1, qa2, statement)`

```python
bridge_validity = await self.check_bridge_validity(
    target,  # 已组合的QA
    neighbor_entity,  # 候选QA
    link_qa.get('statement', '')  # 桥联陈述
)
```

**LLM评估标准**：

1. **概念重叠**：两个QA是否有共同的技术概念？
2. **逻辑联系**：是否有因果关系、对比关系、互补关系？
3. **推理深度**：组合后是否增加推理深度？

**返回结果**：

```python
bridge_validity = {
    'is_valid': True,  # 是否有效
    'relevance_score': 8,  # 相关性分数（0-10）
    'reason': "两个QA都涉及界面缺陷钝化和载流子调控，有明确的因果关系"
}
```

---

#### 步骤3：判断是否接受

**代码**：

```python
relevance_score = bridge_validity.get('relevance_score', 0)

# ⭐ 优化1：放宽阈值（从6降到2）
if relevance_score < 2:
    print(f"✗ 桥联分数过低 ({relevance_score} < 2)")
    continue  # 拒绝

# ⭐ 即使is_valid=False，只要分数>=2就接受
if relevance_score >= 2:
    print(f"✓ 桥联合理 (分数: {relevance_score})")
    # 继续组合...
```

**为什么放宽阈值？**
- 原阈值6太严格（通过率只有20%）
- 降到2后通过率提升到70%
- 即使分数不高，只要有一定相关性就允许组合

---

### 桥联检查的Prompt

**在`prompts_final_new.py`中**：

```python
check_bridge_validity = '''
你是一个半导体领域的专家。评估以下两个QA是否适合桥联（组合）。

【QA1】
问题: {qa1_question}
答案: {qa1_answer}
关键概念: {qa1_concepts}

【QA2】
问题: {qa2_question}
答案: {qa2_answer}
关键概念: {qa2_concepts}

【桥联技术陈述】
{statement}

评估标准：
1. 概念重叠：是否有共同的技术概念？
2. 逻辑联系：是否有因果/对比/互补关系？
3. 推理深度：组合后是否增加推理深度？

输出JSON格式：
{{
    "is_valid": true/false,
    "relevance_score": 0-10,
    "reason": "评估理由（20-40字）"
}}
'''
```

---

### 桥联检查的例子

#### 例子1：高相关性（分数8）

**QA1**：PFM与KPFM在铁电材料表征中的互补性  
**QA2**：有机硅与SiO₂复合介质层对TFT器件的影响  
**桥联陈述**：两种技术都涉及界面工程和材料特性表征

**LLM评估**：

```json
{
  "is_valid": true,
  "relevance_score": 8,
  "reason": "两个QA都涉及界面特性表征，有明确的技术关联"
}
```

**结果**：✅ 通过（分数8 >= 2）

---

#### 例子2：低相关性（分数5）

**QA1**：MOSFET的阈值电压调控  
**QA2**：光刻技术在芯片制造中的应用  
**桥联陈述**：都属于半导体制造流程

**LLM评估**：

```json
{
  "is_valid": false,
  "relevance_score": 5,
  "reason": "有一定技术关联，但逻辑联系较弱"
}
```

**结果**：✅ 仍然通过（分数5 >= 2，放宽阈值）

---

#### 例子3：无相关性（分数1）

**QA1**：硅基太阳能电池的光电转换效率  
**QA2**：DRAM存储器的刷新机制  
**桥联陈述**：都属于半导体器件

**LLM评估**：

```json
{
  "is_valid": false,
  "relevance_score": 1,
  "reason": "两个QA涉及完全不同的应用领域，无明确联系"
}
```

**结果**：❌ 拒绝（分数1 < 2）

---

## 3️⃣ **怎么组合的？完整流程**

### 完整流程图

```
┌─────────────────────────────────────────────┐
│  1. 初始化（第1轮）                         │
│     Action: none                            │
│     ├─ 选择根QA（随机）                     │
│     ├─ 提取信息（关键概念、摘要）           │
│     ├─ 构建基础QA                           │
│     └─ memory.relevant = [QA-1647]          │
│        memory.statements = [statement1]     │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  2. 第2轮（SELECT）                         │
│     Action: SELECT                          │
│     ├─ 检查跳数：1 < 2 ✓                    │
│     ├─ 选择邻居：neighbor_id = 6460         │
│     ├─ 提取邻居信息                         │
│     ├─ 【桥联检查】                         │
│     │   ├─ 生成link_qa（桥联陈述）          │
│     │   ├─ check_bridge_validity            │
│     │   │   └─ relevance_score = 6 >= 2 ✓  │
│     │   └─ 通过                             │
│     ├─ 【信息覆盖检查】                     │
│     │   └─ 不重复 ✓                         │
│     ├─ 【多跳组合】                         │
│     │   ├─ memory_new.relevant = [1647, 6460] │
│     │   ├─ memory_new.statements = [s1, s2]   │
│     │   └─ generate_multihop_question(...)    │
│     │       → 生成2跳问题                    │
│     ├─ 【筛选评估】                         │
│     │   └─ 通过 ✓                           │
│     ├─ 【答案重生成】                       │
│     │   └─ 基于2个子QA重生成答案            │
│     ├─ 更新memory                           │
│     │   memory = memory_new                 │
│     └─ num_hops = 2                         │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  3. 第3轮（SELECT）                         │
│     Action: SELECT                          │
│     ├─ 检查跳数：2 >= 2 ❌                   │
│     └─ 跳过（已达到目标跳数）               │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  4. 第4轮（EXIT）                           │
│     Action: EXIT                            │
│     └─ 退出循环                             │
└─────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────┐
│  5. 保存结果                                │
│     ├─ question: 2跳问题                    │
│     ├─ answer: 基于2个子QA的答案            │
│     ├─ source_qa_ids: [1647, 6460]          │
│     ├─ statements: [s1, s2]                 │
│     ├─ num_hops: 2                          │
│     └─ final_qa_count: 2                    │
└─────────────────────────────────────────────┘
```

---

### 核心代码流程

#### 步骤1：SELECT开始

```python
elif action['action'] == 'SELECT':
    # ⚠️ 检查跳数
    if num_hops >= target_hops:
        print(f"已达到目标跳数 ({target_hops})，跳过")
        continue
    
    # (1) 选择邻居QA
    target = ...  # 从memory.relevant或候选中选择
    candidates = self.kb.find_related_qas_prioritized(target.id, top_k=30)
    neighbor_id = random.choice(candidates[:5])
    neighbor_entity = await self.extract_qa_info(neighbor_id)
```

---

#### 步骤2：桥联检查

```python
    # (2) 生成桥联陈述
    link_qa = await self.build_link_qa(target, neighbor_entity)
    
    # (3) 检查桥联有效性
    bridge_validity = await self.check_bridge_validity(
        target, 
        neighbor_entity, 
        link_qa.get('statement', '')
    )
    
    relevance_score = bridge_validity.get('relevance_score', 0)
    
    if relevance_score < 2:
        print(f"✗ 桥联分数过低 ({relevance_score} < 2)")
        continue  # 拒绝
    
    print(f"✓ 桥联合理 (分数: {relevance_score})")
```

---

#### 步骤3：信息覆盖检查

```python
    # (4) 检查信息是否重复
    duplicate = await self.check_info_cover(
        link_qa['statement'],
        memory_new.statements_repr()
    )
    
    if duplicate:
        print("✗ 信息重复")
        continue
```

---

#### 步骤4：多跳组合

```python
    # (5) 添加新子QA
    memory_new.relevant.append(neighbor_entity)  # 添加QA实体
    memory_new.statements.append(link_qa['statement'])  # 添加statement
    
    # (6) 生成多跳问题
    multihop_result = await self.generate_multihop_question(
        memory_new.relevant,  # 所有子QA
        memory_new.statements  # 所有statements
    )
    
    q_new = multihop_result['question']
    reference_answer = multihop_result['answer']
    reasoning_steps = multihop_result['reasoning_steps']
```

**`generate_multihop_question`内部**：

```python
async def generate_multihop_question(self, all_sub_qas, statements):
    # 格式化所有子QA
    single_hop_str = '\n\n'.join([
        f"【子QA {i+1}】\n问题: {qa.qa_data['question']}\n答案: {qa.qa_data['answer']}"
        for i, qa in enumerate(all_sub_qas)
    ])
    
    # 格式化技术陈述
    statements_str = "\n".join(statements)
    
    # ⭐ 调用LLM生成多跳问题
    prompt = SemiconductorQAPrompts.compose_qa_multihop.format(
        num_hops=len(all_sub_qas),
        single_hop_qas=single_hop_str,
        statements=statements_str
    )
    
    text = await self.call_llm(prompt)
    result = self._safe_json_parse(text)
    
    return {
        'question': result['question'],  # 多跳问题
        'answer': result['answer'],  # 参考答案
        'reasoning_steps': result['reasoning_steps']  # 推理步骤
    }
```

---

#### 步骤5：筛选评估

```python
    # (7) 筛选评估（6个标准）
    eval_result = await self.evaluate_question(memory_new.repr())
    
    if not eval_result['passed']:
        print(f"✗ 未通过筛选：{eval_result['reason']}")
        memory_new.relevant.pop()  # 回滚
        memory_new.statements.pop()
        continue
    
    print(f"✓ 通过筛选")
```

---

#### 步骤6：答案重生成

```python
    # (8) 答案重生成（基于子QA）
    regen_result = await self.regenerate_answer(
        q_new,
        reference_answer,
        memory_new.relevant,  # 所有子QA
        reasoning_steps
    )
    
    final_answer = regen_result['final_answer']
    memory_new.qa['answer'] = final_answer
```

---

#### 步骤7：更新memory

```python
    # (9) 直接接受（优化7：移除测试）
    memory = memory_new  # 更新memory
    ready_to_exit = True
    num_hops += 1
    
    print("[INFO] ✓ 筛选通过，直接接受（已移除测试环节）")
```

---

### 数据流示例

#### 第1轮（初始化）

```python
memory.relevant = [QA-1647]
memory.statements = [
    "PFM与KPFM在铁电材料表征中分别通过机械形变和电势测量..."
]
memory.qa['question'] = "对比PFM与KPFM..."
memory.qa['answer'] = "PFM通过机械形变..."
num_hops = 1
```

---

#### 第2轮（SELECT成功）

```python
# SELECT: 1647 → 6460
memory.relevant = [QA-1647, QA-6460]  # 新增6460
memory.statements = [
    "PFM与KPFM在铁电材料表征中...",
    "有机硅通过氢的钝化抑制SiO₂界面缺陷..."  # 新增
]
memory.qa['question'] = "在低温制备TFT器件中，为何交替使用有机硅与SiO₂..."  # 2跳问题
memory.qa['answer'] = "交替使用有机硅与SiO₂复合介质层通过氢的钝化..."  # 基于2个子QA
num_hops = 2
```

---

#### 第3轮（SELECT跳过）

```python
# 检查：num_hops (2) >= target_hops (2) → 跳过
# memory不变
```

---

#### 第4轮（EXIT）

```python
# LLM选择EXIT → 退出循环
# 保存结果：
{
  "question": "2跳问题",
  "answer": "基于2个子QA的答案",
  "source_qa_ids": [1647, 6460],
  "statements": [s1, s2],
  "num_hops": 2,
  "final_qa_count": 2
}
```

---

## 🎯 **总结**

### 1. Statements的作用

| 作用 | 说明 |
|------|------|
| **生成上下文** | 提供给LLM生成多跳问题的简洁技术陈述 |
| **信息去重** | 检查新QA是否与已有statements重复 |
| **展示概览** | 在`memory.repr()`中展示技术关系 |
| **逻辑连接** | 体现QA之间的技术联系 |

---

### 2. 桥联检查的逻辑

| 步骤 | 说明 |
|------|------|
| **生成link_qa** | 描述两个QA的技术联系 |
| **评估相关性** | LLM打分（0-10） |
| **判断阈值** | 分数 >= 2 就接受（放宽） |
| **目的** | 确保组合的QA有逻辑联系 |

---

### 3. 组合流程

```
选择邻居
  ↓
桥联检查（相关性 >= 2）
  ↓
信息去重（不重复）
  ↓
多跳组合（生成新问题）
  ↓
筛选评估（6个标准）
  ↓
答案重生成（基于子QA）
  ↓
直接接受（移除测试）
  ↓
更新memory
```

---

### 关键点

1. **Statements**：简洁概括QA的技术关系
2. **桥联检查**：确保组合的QA有逻辑联系（分数 >= 2）
3. **多跳组合**：基于所有子QA和statements生成新问题
4. **放宽阈值**：桥联从6降到2，测试移除，提升多跳成功率

---

**理解了吗？** 🎓
