# 🔍 诊断：相关QA实体列表只有1个

**问题**：用户看到`memory.relevant`只有1个QA  
**是否Bug**：❌ 不一定是Bug！需要诊断  
**诊断时间**：2025-11-19

---

## 💡 **关键理解**

### `memory.relevant`的真实含义

```
相关QA实体列表：
- [QA-8691] (ID: 8691)  ← 这是什么？
```

**不是**：
- ❌ "可以选择的候选QA"（候选有30个）
- ❌ "知识库中所有相关的QA"

**而是**：
- ✅ **"当前多跳问题已经组合进去的源QA"**
- ✅ 初始化后只有1个root_entity是**正常的**！

---

## 📊 **正常vs异常**

### ✅ 正常情况1：初始化后

```
============================================================
[START] 根实体: QA-8691
============================================================

    [提取] 实体 8691
    [初始化] memory.relevant初始化为1个：[8691]  ← ✅ 正常
    [初始化] 该实体有 10 个相关QA可供选择  ← ✅ 候选充足
    [构建] 基础QA

[BASE QA] 在熔盐辅助CVD法...

--- 第 1 轮 ---
[ACTION] none -  ← 第1轮不执行action

相关QA实体列表：
- [QA-8691] (ID: 8691)  ← ✅ 正常！初始化后只有1个
```

**判断**：✅ 这是正常的！

---

### ✅ 正常情况2：SELECT成功后增加

```
--- 第 2 轮 ---
[ACTION] SELECT - ...
  [SELECT] ===== 开始SELECT流程 =====
  [SELECT] 找到 30 个候选邻居  ← ✅ 找到候选
  [SELECT] 排除已存在的，剩余 29 个候选  ← ✅ 过滤后还有很多
  [SELECT] 8691 → 2048 (从前5个候选中选择)
  [SELECT] ✓ 桥联合理 (分数: 8)
  [多跳组合] ✓ 成功生成2跳问题
  [筛选] ✓ 通过
  [TEST] 正确率: 3/4  ← ✅ 测试通过
  [INFO] 测试通过

--- 第 3 轮 ---
相关QA实体列表：
- [QA-8691] (ID: 8691)
- [QA-2048] (ID: 2048)  ← ✅ 增加了！
```

**判断**：✅ SELECT成功且测试通过，会增加新QA

---

### ❌ 异常情况1：KB太小（无法补充）

```
[ACTION] SELECT - ...
  [SELECT] ===== 开始SELECT流程 =====
  [SELECT] 找到 1 个候选邻居  ← ❌ 只有1个
  [KB] 保底补充：embedding找到1个，补充0个，总计1个  ← ❌ 无法补充
  [KB] ⚠️ 无可补充的QA（KB总数: 2）  ← ❌ KB太小
  [SELECT] 排除已存在的，剩余 0 个候选
  [SELECT] ✗ 无可用邻居

相关QA实体列表：
- [QA-8691] (ID: 8691)  ← 还是1个
```

**诊断**：❌ KB中QA数量太少（只有2个）

**解决**：
```bash
# 检查QA数量
wc -l /mnt/workspace/LLM/ldd/多跳数据/data/QA.jsonl

# 建议：至少100个QA
```

---

### ❌ 异常情况2：保底机制正常，但候选都被排除

```
[ACTION] SELECT - ...
  [SELECT] ===== 开始SELECT流程 =====
  [SELECT] 找到 30 个候选邻居  ← ✅ 找到候选
  [KB] 保底补充：embedding找到5个，补充25个，总计30个  ← ✅ 保底生效
  [SELECT] 排除已存在的，剩余 0 个候选  ← ❌ 全部被排除
  [SELECT] ✗ 无可用邻居

相关QA实体列表：
- [QA-8691] (ID: 8691)  ← 还是1个
```

**诊断**：❌ 所有候选都在memory.relevant中（已经用过了）

**原因**：这个不应该发生，因为初始只有1个实体

---

### ❌ 异常情况3：SELECT都失败（桥联/筛选）

```
--- 第 2-16 轮 ---
[ACTION] SELECT - ...
  [SELECT] 找到 30 个候选邻居  ← ✅ 候选充足
  [SELECT] 排除已存在的，剩余 29 个候选
  [SELECT] 8691 → 2048
  [SELECT] ✗ 桥联分数过低 (1 < 3)  ← ❌ 桥联失败

或者
  [SELECT] ✗ 未通过筛选  ← ❌ 筛选失败

相关QA实体列表：
- [QA-8691] (ID: 8691)  ← 还是1个
```

**诊断**：❌ 桥联或筛选太严

**解决**：进一步放宽（见下面）

---

### ❌ 异常情况4：测试未通过（最可能）

```
--- 第 2-16 轮 ---
[ACTION] SELECT - ...
  [SELECT] 找到 30 个候选邻居  ← ✅ 候选充足
  [多跳组合] ✓ 成功
  [筛选] ✓ 通过
  [TEST] 正确率: 0/4  ← ❌ 测试未通过
  [TEST] 正确率: 1/4  ← ❌ 测试未通过
  [INFO] 测试未通过

相关QA实体列表：
- [QA-8691] (ID: 8691)  ← 还是1个
```

**诊断**：❌ 测试标准太严（需要2/4，但只有0-1/4）

**解决**：降低测试标准（见下面）

---

## 🔧 **解决方案（按诊断结果）**

### 解决方案1：KB太小 → 无法解决，需要更多数据

```bash
# 检查
wc -l /path/to/QA.jsonl

# 如果<100，建议添加更多QA到知识库
```

---

### 解决方案2：进一步放宽桥联阈值（3→2）

<function_calls>
<invoke name="StrReplace">
<parameter name="file_path">/workspace/agent_final_new.py